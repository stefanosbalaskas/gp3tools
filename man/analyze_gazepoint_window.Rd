% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/window_aoi_extensions.R
\name{analyze_gazepoint_window}
\alias{analyze_gazepoint_window}
\title{Summarise gaze or pupil measures in sliding time windows}
\usage{
analyze_gazepoint_window(
  et_data,
  window_size = 50,
  step = 10,
  summary_stats = c("mean", "sd"),
  by = "USER_ID",
  condition_col = NULL,
  value_cols = NULL,
  ts_col = "TIME",
  window_unit = c("milliseconds", "seconds", "native"),
  time_unit = c("auto", "seconds", "milliseconds"),
  include_partial = FALSE
)
}
\arguments{
\item{et_data}{A sample-level data frame.}

\item{window_size}{Window width.}

\item{step}{Distance between consecutive window starts.}

\item{summary_stats}{Statistics to calculate. Supported values are
\code{"mean"}, \code{"sd"}, \code{"median"}, \code{"min"}, \code{"max"}, \code{"sum"}, and
\code{"valid_prop"}.}

\item{by}{Grouping columns defining independent time series.}

\item{condition_col}{Optional condition column appended to \code{by}.}

\item{value_cols}{Numeric columns to summarise. When \code{NULL}, common gaze and
pupil columns are detected.}

\item{ts_col}{Timestamp column.}

\item{window_unit}{Unit used by \code{window_size} and \code{step}.}

\item{time_unit}{Unit of the timestamp column.}

\item{include_partial}{Include a final window that is shorter than
\code{window_size}.}
}
\value{
A tibble with one row per group and time window.
}
\description{
Creates overlapping or non-overlapping time windows and calculates selected
summary statistics for numeric gaze or pupil columns.
}
\examples{
pupil <- data.frame(
  USER_ID = "P01",
  TIME = seq(0, 0.99, by = 0.01),
  mean_pupil = sin(seq(0, 2 * pi, length.out = 100))
)
analyze_gazepoint_window(
  pupil,
  window_size = 100,
  step = 50,
  value_cols = "mean_pupil"
)
}
