% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/face_quality_audit.R
\name{audit_gazepoint_face_quality}
\alias{audit_gazepoint_face_quality}
\title{Audit external facial-behaviour data quality}
\usage{
audit_gazepoint_face_quality(
  data,
  group_cols = c("participant_id", "face_file"),
  confidence_threshold = 0.8,
  min_valid_percent = 70,
  warning_valid_percent = 85,
  max_time_gap_sec = NULL,
  max_duplicate_frame_percent = 1,
  standardize = TRUE
)
}
\arguments{
\item{data}{A data frame returned by \code{standardize_gazepoint_face_columns()},
a data frame that can be standardised by that function, or a path to a CSV
file readable by \code{read_gazepoint_face_export()}.}

\item{group_cols}{Character vector of grouping columns for quality summaries.
Columns not present in \code{data} are ignored. Use \code{NULL} for an overall-only
audit.}

\item{confidence_threshold}{Minimum face-detection confidence used when
standardising unstandardised data.}

\item{min_valid_percent}{Minimum valid-row percentage below which a group is
marked as \code{"fail"}.}

\item{warning_valid_percent}{Valid-row percentage below which a group is
marked as \code{"warn"} when it is still above \code{min_valid_percent}.}

\item{max_time_gap_sec}{Optional maximum allowed time gap in seconds. If
supplied, groups with larger observed positive gaps are marked as \code{"warn"}.}

\item{max_duplicate_frame_percent}{Maximum tolerated percentage of duplicate
non-missing frame indices before a group is marked as \code{"warn"}.}

\item{standardize}{Should unstandardised data be passed through
\code{standardize_gazepoint_face_columns()} before auditing?}
}
\value{
A list with \code{overview}, \code{group_summary}, \code{issue_summary}, \code{data}, and
\code{settings}. The returned object has class \code{gp3_face_quality_audit}.
}
\description{
Audits the quality of standardised external facial-behaviour data imported
with \code{read_gazepoint_face_export()} and standardised with
\code{standardize_gazepoint_face_columns()}. The helper checks face-detection
validity, confidence, success, duplicate frame indices, and basic timing
continuity. It does not infer facial expressions or emotional states.
}
\examples{
face <- data.frame(
  frame = 1:3,
  timestamp = c(0, 0.033, 0.066),
  confidence = c(0.95, 0.90, 0.40),
  success = c(1, 1, 1),
  AU12_r = c(0.1, 0.2, 0.3)
)

audit_gazepoint_face_quality(face)
}
