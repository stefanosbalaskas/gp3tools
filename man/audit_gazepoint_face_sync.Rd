% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/face_sync.R
\name{audit_gazepoint_face_sync}
\alias{audit_gazepoint_face_sync}
\title{Audit synchronisation between Gazepoint and external facial-behaviour data}
\usage{
audit_gazepoint_face_sync(
  data,
  group_cols = NULL,
  min_matched_percent = 70,
  warning_matched_percent = 85,
  max_abs_diff_sec = NULL
)
}
\arguments{
\item{data}{A data frame returned by \code{sync_gazepoint_face_data()}.}

\item{group_cols}{Optional character vector of grouping columns. Columns not
present in \code{data} are ignored. Use \code{NULL} for an overall-only audit.}

\item{min_matched_percent}{Minimum percentage of rows that must have
\code{face_sync_status == "matched"} for a group to pass.}

\item{warning_matched_percent}{Percentage below which a group is marked as
\code{"warn"} when still above \code{min_matched_percent}.}

\item{max_abs_diff_sec}{Optional maximum allowed absolute synchronisation
difference in seconds. Only evaluated when \code{face_sync_abs_diff_sec} is
available.}
}
\value{
A list with \code{overview}, \code{group_summary}, \code{issue_summary}, \code{data}, and
\code{settings}. The returned object has class \code{gp3_face_sync_audit}.
}
\description{
Summarises the output of \code{sync_gazepoint_face_data()} by reporting matched,
unmatched, outside-tolerance, and missing-timing/frame rows. For nearest-time
synchronisation, it also reports absolute time-difference summaries. The
helper audits alignment quality only; it does not infer facial expressions or
emotional states.
}
\examples{
gaze <- data.frame(
  subject_id = "P001",
  time_sec = c(0.00, 0.03, 0.07)
)

face <- data.frame(
  participant_id = "P001",
  frame = 1:3,
  timestamp = c(0.00, 0.033, 0.066),
  confidence = c(0.95, 0.94, 0.93),
  success = c(1, 1, 1)
)

synced <- sync_gazepoint_face_data(
  gaze,
  face,
  by = c(subject_id = "participant_id"),
  gaze_time_col = "time_sec"
)

audit_gazepoint_face_sync(synced)
}
