% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_gazepoint_pupil_drift.R
\name{audit_gazepoint_pupil_drift}
\alias{audit_gazepoint_pupil_drift}
\title{Audit Gazepoint pupil drift}
\usage{
audit_gazepoint_pupil_drift(
  data,
  group_cols = "subject",
  pupil_col = NULL,
  time_col = "time",
  order_col = "trial",
  condition_col = "condition",
  exclude_col = "excluded_trial",
  include_excluded = FALSE,
  min_valid_samples = 3,
  max_abs_slope_per_min = 1,
  max_condition_time_mean_diff_ms = 1000,
  max_condition_order_mean_diff = 1
)
}
\arguments{
\item{data}{A data frame from a Gazepoint pupil preprocessing pipeline.}

\item{group_cols}{Character vector of grouping columns for the main drift
audit. The default is \code{"subject"}.}

\item{pupil_col}{Name of the pupil column to analyse. If \code{NULL}, the
function automatically tries \code{pupil_smoothed},
\code{pupil_baseline_corrected}, \code{pupil_interpolated}, \code{pupil_clean}, and
\code{pupil}.}

\item{time_col}{Name of the within-trial or sample-time column.}

\item{order_col}{Optional trial/order column used to assess time-on-task
imbalance. If \code{NULL}, order-based summaries are skipped.}

\item{condition_col}{Optional condition column used to summarise condition
drift and time-on-task imbalance. If \code{NULL}, condition summaries are
skipped.}

\item{exclude_col}{Optional logical exclusion column. If present and
\code{include_excluded = FALSE}, excluded rows are removed before analysis.}

\item{include_excluded}{Logical. If \code{FALSE}, rows marked by \code{exclude_col}
are excluded when that column exists.}

\item{min_valid_samples}{Minimum valid pupil samples required to estimate a
drift slope.}

\item{max_abs_slope_per_min}{Maximum acceptable absolute pupil slope per
minute before a drift warning is raised.}

\item{max_condition_time_mean_diff_ms}{Maximum acceptable difference in mean
sample time across conditions.}

\item{max_condition_order_mean_diff}{Maximum acceptable difference in mean
trial/order value across conditions.}
}
\value{
A named list containing \code{by_group}, \code{by_subject}, \code{by_condition},
\code{condition_balance}, and \code{summary} tibbles.
}
\description{
Summarise tonic pupil/time-on-task drift in processed Gazepoint pupil data.
}
\details{
The function estimates simple linear pupil trends over time within selected
grouping variables, usually subjects. It also reports subject-level drift,
condition-level drift, and possible condition imbalance in time-on-task.
}
