% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline_correct_gazepoint_pupil.R
\name{baseline_correct_gazepoint_pupil}
\alias{baseline_correct_gazepoint_pupil}
\title{Baseline-correct Gazepoint pupil data}
\usage{
baseline_correct_gazepoint_pupil(
  data,
  pupil_col = NULL,
  time_col = NULL,
  baseline_time_col = NULL,
  baseline_window = c(-200, 0),
  baseline_flag_col = NULL,
  group_cols = c("subject", "media_id"),
  baseline_method = c("mean", "median"),
  min_baseline_samples = 1
)
}
\arguments{
\item{data}{A Gazepoint master table, preferably after
\code{\link[=interpolate_gazepoint_pupil]{interpolate_gazepoint_pupil()}}.}

\item{pupil_col}{Optional name of the pupil column to baseline-correct. If
\code{NULL}, the function detects one of \code{pupil_interpolated},
\code{pupil_for_preprocessing}, \code{mean_pupil}, \code{pupil}, \code{pupil_raw},
\code{left_pupil}, or \code{right_pupil}.}

\item{time_col}{Optional name of the main time column. If \code{NULL}, the
function detects one of \code{time_ms}, \code{time}, \code{time_orig}, or \code{time_orig_ms}.}

\item{baseline_time_col}{Optional name of the time column used for selecting
baseline samples. If \code{NULL}, the function detects relative-time columns
first, then falls back to \code{time_col}.}

\item{baseline_window}{Numeric vector of length two giving the baseline
window in milliseconds. Defaults to \code{c(-200, 0)}. This can also be set to
post-onset or early-window values such as \code{c(0, 200)} when no pre-stimulus
period is available.}

\item{baseline_flag_col}{Optional logical column identifying baseline samples.
If supplied, this takes priority over \code{baseline_window}.}

\item{group_cols}{Character vector of grouping columns used to compute one
baseline per independent time series. Defaults to \code{c("subject", "media_id")}. Columns such as \code{"trial"} or \code{"trial_global"} can be added
when available. Use \code{character(0)} for one global baseline.}

\item{baseline_method}{Baseline statistic. One of \code{"mean"} or \code{"median"}.
Defaults to \code{"mean"}.}

\item{min_baseline_samples}{Minimum number of valid baseline samples required
to compute a baseline. Defaults to \code{1}.}
}
\value{
A tibble containing the original data plus baseline-correction
columns.
}
\description{
Computes baseline-corrected pupil columns from a Gazepoint pupil time series,
typically after \code{\link[=flag_gazepoint_pupil]{flag_gazepoint_pupil()}} and \code{\link[=interpolate_gazepoint_pupil]{interpolate_gazepoint_pupil()}}.
Baselines can be defined either by a time window, such as \code{c(-200, 0)}, or by
a user-supplied logical baseline/pre-stimulus flag column.
}
\examples{
\dontrun{
flagged <- flag_gazepoint_pupil(master)
interpolated <- interpolate_gazepoint_pupil(flagged)

corrected <- baseline_correct_gazepoint_pupil(
  interpolated,
  baseline_window = c(-200, 0)
)

corrected <- baseline_correct_gazepoint_pupil(
  interpolated,
  baseline_window = c(0, 200)
)
}

}
