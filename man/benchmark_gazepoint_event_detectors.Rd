% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_detector_benchmarking.R
\name{benchmark_gazepoint_event_detectors}
\alias{benchmark_gazepoint_event_detectors}
\title{Benchmark Gazepoint event detectors against reviewed events}
\usage{
benchmark_gazepoint_event_detectors(
  x,
  reviewed_events,
  sequence_cols = NULL,
  reviewed_start_col = "start_time",
  reviewed_end_col = "end_time",
  reviewed_id_col = "review_event_id",
  review_status_col = "review_status",
  accepted_status = c("accepted", "include", "reviewed", "confirmed"),
  min_overlap = 0.5,
  time_unit = c("seconds", "milliseconds")
)
}
\arguments{
\item{x}{An object returned by \code{\link[=compare_gazepoint_event_detectors]{compare_gazepoint_event_detectors()}} or a
standardized detector-event data frame.}

\item{reviewed_events}{A data frame containing reviewed reference intervals.}

\item{sequence_cols}{Sequence identifier columns. When \code{x} is a comparison
object, its stored sequence columns are used by default.}

\item{reviewed_start_col, reviewed_end_col}{Start and end columns in
\code{reviewed_events}.}

\item{reviewed_id_col}{Optional reviewed-event identifier column.}

\item{review_status_col}{Optional review-status column. When present, only
rows whose status is included in \code{accepted_status} are used.}

\item{accepted_status}{Character values treated as accepted reviews.}

\item{min_overlap}{Minimum interval intersection-over-union required for a
true-positive match.}

\item{time_unit}{Unit used by event start/end values. This controls conversion
of timing errors to milliseconds.}
}
\value{
An object of class \code{"gp3_event_detector_benchmark"} containing
detector-level metrics, sequence-level metrics, one-to-one matches,
unmatched-event diagnostics, accepted reviewed events, detector events,
detector-run information, and settings.
}
\description{
Compare standardized fixation intervals from one or more detectors with a
manually reviewed or synthetic reference-event table. Matching is one-to-one
within each participant/trial sequence and is based on interval
intersection-over-union. Results quantify methodological agreement with the
supplied reference annotations; they do not establish a universally correct
detector.
}
\examples{
reviewed <- data.frame(
  USER_ID = "P01",
  trial = "T01",
  review_event_id = 1:2,
  start_time = c(0, 2),
  end_time = c(1, 3)
)

detected <- data.frame(
  USER_ID = "P01",
  trial = "T01",
  detector = "velocity_10",
  family = "velocity",
  threshold = 10,
  event_id = 1:2,
  start_time = c(0.05, 2.05),
  end_time = c(1.05, 3.05),
  duration_ms = 1000
)

benchmark <- benchmark_gazepoint_event_detectors(
  detected,
  reviewed,
  sequence_cols = c("USER_ID", "trial")
)

benchmark$detector_metrics
}
