% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scanpath_cluster_stability.R
\name{bootstrap_gazepoint_scanpath_clusters}
\alias{bootstrap_gazepoint_scanpath_clusters}
\title{Bootstrap scanpath-cluster stability}
\usage{
bootstrap_gazepoint_scanpath_clusters(
  x,
  k = 3L,
  n_boot = 200L,
  sample_fraction = 0.8,
  method = c("hierarchical", "pam"),
  linkages = "average",
  seed = NULL,
  aoi_col = NULL,
  group_cols = NULL,
  time_col = NULL,
  distance_col = "normalized_distance",
  include_missing = FALSE,
  missing_label = "missing",
  collapse_repeats = FALSE,
  max_sequences = 200
)
}
\arguments{
\item{x}{Long-format AOI data, pairwise distance data, a square numeric
distance matrix, or a \code{"dist"} object.}

\item{k}{Number of clusters.}

\item{n_boot}{Number of subsampling iterations per specification.}

\item{sample_fraction}{Proportion of scanpaths retained in each iteration.
The resolved sample size is always at least \code{k + 1}.}

\item{method}{Clustering method: \code{"hierarchical"} or \code{"pam"}.}

\item{linkages}{Hierarchical linkage methods to compare. Ignored for PAM.}

\item{seed}{Optional integer seed. The caller's random-number state is
restored on exit.}

\item{aoi_col}{AOI column when \code{x} is long-format AOI data.}

\item{group_cols}{Columns identifying independent scanpaths.}

\item{time_col}{Optional ordering column.}

\item{distance_col}{Distance column for pairwise-distance data.}

\item{include_missing}{Should missing AOI labels be retained?}

\item{missing_label}{Label used for retained missing AOIs.}

\item{collapse_repeats}{Should consecutive repeated AOIs be collapsed?}

\item{max_sequences}{Maximum number of scanpaths permitted when pairwise
distances must be calculated.}
}
\value{
An object of class \code{"gp3_scanpath_cluster_bootstrap"} containing
full-data reference fits, co-clustering and pair-coverage matrices,
iteration-level adjusted Rand results, representative stability, the
reusable distance object, and resolved settings.
}
\description{
Evaluate the stability of scanpath clustering by repeatedly subsampling
scanpaths, refitting the clustering solution, and recording co-clustering,
adjusted Rand agreement, and representative-scanpath selection.
}
\details{
The routine reuses the distance formats accepted by
\code{\link[=cluster_gazepoint_scanpaths]{cluster_gazepoint_scanpaths()}}. Hierarchical solutions can be compared
across multiple linkage methods. PAM remains optional through \pkg{cluster}.
}
\examples{
latent <- rep(1:3, each = 2)
d <- outer(
  latent,
  latent,
  FUN = function(x, y) ifelse(x == y, 0.1, 1)
)
diag(d) <- 0
dimnames(d) <- list(LETTERS[1:6], LETTERS[1:6])

stability <- bootstrap_gazepoint_scanpath_clusters(
  d,
  k = 3,
  n_boot = 10,
  seed = 1
)

stability$iteration_summary
}
