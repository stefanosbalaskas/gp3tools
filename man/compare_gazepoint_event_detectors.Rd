% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_detector_comparison.R
\name{compare_gazepoint_event_detectors}
\alias{compare_gazepoint_event_detectors}
\title{Compare Gazepoint event-detection workflows}
\usage{
compare_gazepoint_event_detectors(
  data,
  id_col = "USER_ID",
  trial_col = NULL,
  group_cols = NULL,
  x_col = "FPOGX",
  y_col = "FPOGY",
  time_col = "TIME",
  methods = c("velocity", "hmm", "eyetools"),
  velocity_thresholds = c(5, 10, 20),
  min_duration = 50,
  hmm_states = 3L,
  eyetools_method = c("vti", "dispersion"),
  run_optional_eyetools = FALSE,
  min_overlap = 0.5,
  velocity_args = list(),
  hmm_args = list(),
  eyetools_args = list()
)
}
\arguments{
\item{data}{A sample-level gaze data frame.}

\item{id_col}{Participant identifier column.}

\item{trial_col}{Optional trial identifier column.}

\item{group_cols}{Optional additional sequence columns.}

\item{x_col, y_col}{Gaze-coordinate columns.}

\item{time_col}{Timestamp column.}

\item{methods}{Detector branches to run: \code{"velocity"}, \code{"hmm"}, and
\code{"eyetools"}.}

\item{velocity_thresholds}{One or more positive velocity thresholds.}

\item{min_duration}{Minimum fixation duration in milliseconds for the native
velocity branch.}

\item{hmm_states}{Number of HMM states.}

\item{eyetools_method}{eyetools detector method.}

\item{run_optional_eyetools}{Should the optional eyetools branch be run?}

\item{min_overlap}{Minimum intersection-over-union used to classify
overlapping events as matched.}

\item{velocity_args}{Named list overriding native velocity-detector
defaults.}

\item{hmm_args}{Named list overriding HMM-classifier defaults.}

\item{eyetools_args}{Named list overriding eyetools-wrapper defaults.}
}
\value{
An object of class \code{"gp3_event_detector_comparison"} containing
standardized event tables, detector-run status, detector summaries,
pairwise agreement, unmatched events, raw detector outputs, and settings.
}
\description{
Run native velocity-threshold detection, the lightweight gp3tools HMM
classifier, and an optional eyetools branch on the same sample-level gaze
data. Detector failures are recorded without invalidating successful
branches.
}
\examples{
gaze <- data.frame(
  USER_ID = rep("P01", 80),
  trial = rep("T01", 80),
  TIME = seq(0, by = 0.01, length.out = 80),
  FPOGX = c(rep(0.2, 30), seq(0.2, 0.8, length.out = 10), rep(0.8, 40)),
  FPOGY = 0.5
)

comparison <- compare_gazepoint_event_detectors(
  gaze,
  trial_col = "trial",
  methods = "velocity",
  velocity_thresholds = c(5, 10)
)

comparison$detector_summary
}
