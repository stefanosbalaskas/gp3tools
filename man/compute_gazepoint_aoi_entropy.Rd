% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aoi_entropy.R
\name{compute_gazepoint_aoi_entropy}
\alias{compute_gazepoint_aoi_entropy}
\title{Compute AOI entropy metrics}
\usage{
compute_gazepoint_aoi_entropy(
  data,
  aoi_col,
  group_cols = NULL,
  time_col = NULL,
  include_missing = FALSE,
  missing_label = "missing",
  collapse_repeats = FALSE,
  log_base = 2
)
}
\arguments{
\item{data}{A data frame containing AOI observations.}

\item{aoi_col}{Character scalar. Column containing AOI labels.}

\item{group_cols}{Optional character vector of grouping columns, such as
participant, trial, stimulus, or condition columns.}

\item{time_col}{Optional character scalar. If supplied, observations are
ordered by this column within each group before transitions are computed.}

\item{include_missing}{Logical. If \code{TRUE}, missing or empty AOI labels are
retained as \code{missing_label}; otherwise they are removed.}

\item{missing_label}{Character scalar used when \code{include_missing = TRUE}.}

\item{collapse_repeats}{Logical. If \code{TRUE}, consecutive identical AOI labels
are collapsed before transition entropy is computed.}

\item{log_base}{Numeric scalar. Base of the logarithm used for entropy.}
}
\value{
A data frame with one row per group and entropy/count columns.
}
\description{
Computes spatial AOI entropy, directed transition entropy, and conditional
transition entropy for Gazepoint-style AOI sequences. The function is useful
for quantifying how concentrated, dispersed, or predictable gaze allocation is
across Areas of Interest.
}
\examples{
dat <- data.frame(
  subject = "S01",
  trial = "T01",
  time = 1:6,
  AOI = c("A", "A", "B", "C", "B", "A")
)

compute_gazepoint_aoi_entropy(
  dat,
  aoi_col = "AOI",
  group_cols = c("subject", "trial"),
  time_col = "time"
)
}
