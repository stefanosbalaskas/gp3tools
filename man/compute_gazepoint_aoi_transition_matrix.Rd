% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_gazepoint_aoi_transition_matrix.R
\name{compute_gazepoint_aoi_transition_matrix}
\alias{compute_gazepoint_aoi_transition_matrix}
\title{Compute Gazepoint AOI transition matrices}
\usage{
compute_gazepoint_aoi_transition_matrix(
  data,
  aoi_col = NULL,
  time_col = "time",
  group_cols = c("subject", "MEDIA_ID", "trial_global"),
  by_cols = NULL,
  include_non_aoi = TRUE,
  include_self_transitions = TRUE,
  states = NULL,
  time_window = NULL,
  non_aoi_values = c("non_aoi", "none", "background", "outside", "outside_aoi",
    "missing", "missing_aoi"),
  missing_aoi_label = "missing_aoi"
)
}
\arguments{
\item{data}{A Gazepoint sample-level data frame, AOI-entry table, or
AOI-sequence table.}

\item{aoi_col}{Name of the AOI-state column. Used only when \code{data} is
sample-level data. If \code{NULL}, the function tries \code{aoi_current}, \code{AOI},
and \code{aoi_state}.}

\item{time_col}{Name of the time column, in milliseconds. Used only when
\code{data} is sample-level data.}

\item{group_cols}{Character vector of columns defining independent AOI
sequences, usually subject/media/trial.}

\item{by_cols}{Optional character vector of columns used to compute separate
matrices, for example \code{condition} or \code{MEDIA_ID}.}

\item{include_non_aoi}{Logical. If \code{TRUE}, non-AOI/background states are
included in the transition matrix.}

\item{include_self_transitions}{Logical. If \code{TRUE}, same-state transitions
are retained. These can occur after non-AOI states are removed.}

\item{states}{Optional character vector giving the desired row/column order
for the transition matrices.}

\item{time_window}{Optional numeric vector of length 2 giving an entry-start
time window in milliseconds.}

\item{non_aoi_values}{Character vector of AOI labels treated as background
or non-AOI states.}

\item{missing_aoi_label}{Label used when the AOI value is missing.}
}
\value{
A list containing count matrices, probability matrices, and long-form
transition counts/probabilities.
}
\description{
Compute AOI transition count and probability matrices from sample-level
Gazepoint AOI data, AOI-entry tables, or AOI-sequence tables. The function
returns both matrix and long-table forms.
}
