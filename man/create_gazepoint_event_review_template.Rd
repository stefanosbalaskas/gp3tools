% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_detector_benchmarking.R
\name{create_gazepoint_event_review_template}
\alias{create_gazepoint_event_review_template}
\title{Create a manual event-review template}
\usage{
create_gazepoint_event_review_template(
  data,
  id_col = "USER_ID",
  trial_col = NULL,
  group_cols = NULL,
  time_col = "TIME",
  rows_per_sequence = 1L,
  event_type = "fixation",
  reviewer = NA_character_
)
}
\arguments{
\item{data}{Sample-level gaze data.}

\item{id_col}{Participant identifier column.}

\item{trial_col}{Optional trial identifier column.}

\item{group_cols}{Optional additional sequence columns.}

\item{time_col}{Timestamp column.}

\item{rows_per_sequence}{Number of placeholder rows created per sequence.}

\item{event_type}{Event label inserted in the template.}

\item{reviewer}{Optional reviewer identifier.}
}
\value{
A data frame suitable for export to CSV and manual review.
}
\description{
Create a sequence-level CSV-ready template for manually annotating fixation
intervals. The returned table contains one or more placeholder rows per
participant/trial sequence together with the observed sequence boundaries.
Reviewers should fill \code{start_time} and \code{end_time}, set \code{review_status} to
\code{"accepted"}, and add rows when a sequence contains multiple events.
}
\examples{
gaze <- data.frame(
  USER_ID = rep(c("P01", "P02"), each = 10),
  trial = rep("T01", 20),
  TIME = rep(seq(0, 0.09, by = 0.01), 2),
  FPOGX = 0.5,
  FPOGY = 0.5
)

template <- create_gazepoint_event_review_template(
  gaze,
  trial_col = "trial"
)

template
}
