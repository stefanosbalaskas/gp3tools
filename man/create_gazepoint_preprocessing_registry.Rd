% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_gazepoint_preprocessing_registry.R
\name{create_gazepoint_preprocessing_registry}
\alias{create_gazepoint_preprocessing_registry}
\title{Create a Gazepoint pupil-preprocessing registry}
\usage{
create_gazepoint_preprocessing_registry(
  blink_padding_pre_ms = 100,
  blink_padding_post_ms = 100,
  max_interpolation_gap_ms = 150,
  smoothing_window_ms = 50,
  baseline_start_ms = -200,
  baseline_end_ms = 0,
  pupil_physiological_min = 1,
  pupil_physiological_max = 9,
  pupil_speed_mad_k = 6,
  binocular_mad_k = 6,
  baseline_missing_prop_threshold = 0.3,
  baseline_interpolated_prop_threshold = 0.3,
  baseline_artifact_prop_threshold = 0.3,
  overlap_trial_duration_ms = 3000,
  overlap_event_gap_ms = 1000
)
}
\arguments{
\item{blink_padding_pre_ms}{Padding before bad pupil samples, blinks, or
tracking artifacts, in milliseconds. Defaults to \code{100}.}

\item{blink_padding_post_ms}{Padding after bad pupil samples, blinks, or
tracking artifacts, in milliseconds. Defaults to \code{100}.}

\item{max_interpolation_gap_ms}{Maximum missing-pupil gap duration to
interpolate, in milliseconds. Defaults to \code{150}.}

\item{smoothing_window_ms}{Rolling smoothing window, in milliseconds.
Defaults to \code{50}.}

\item{baseline_start_ms}{Baseline-window start, in milliseconds. Defaults to
\code{-200}.}

\item{baseline_end_ms}{Baseline-window end, in milliseconds. Defaults to \code{0}.}

\item{pupil_physiological_min}{Minimum plausible pupil value when the pupil
unit is known to be millimetres. Defaults to \code{1}.}

\item{pupil_physiological_max}{Maximum plausible pupil value when the pupil
unit is known to be millimetres. Defaults to \code{9}.}

\item{pupil_speed_mad_k}{MAD multiplier for pupil-speed outlier detection.
Defaults to \code{6}.}

\item{binocular_mad_k}{MAD multiplier for left-right pupil disagreement.
Defaults to \code{6}.}

\item{baseline_missing_prop_threshold}{Baseline missingness threshold used
for baseline-quality audits. Defaults to \code{0.30}.}

\item{baseline_interpolated_prop_threshold}{Baseline interpolation threshold
used for baseline-quality audits. Defaults to \code{0.30}.}

\item{baseline_artifact_prop_threshold}{Baseline artifact threshold used for
baseline-quality audits. Defaults to \code{0.30}.}

\item{overlap_trial_duration_ms}{Trial-duration threshold below which pupil
overlap/deconvolution risk should be considered. Defaults to \code{3000}.}

\item{overlap_event_gap_ms}{Event-gap threshold below which pupil-response
overlap should be considered. Defaults to \code{1000}.}
}
\value{
A tibble with one row per preprocessing parameter.
}
\description{
Creates a compact registry of commonly used preprocessing parameters for
Gazepoint pupil and gaze analyses. The registry is designed to make
preprocessing choices explicit, auditable, and easy to report.
}
\examples{
registry <- create_gazepoint_preprocessing_registry()
registry

}
