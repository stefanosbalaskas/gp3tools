% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_detection_extensions.R
\name{detect_gazepoint_blinks}
\alias{detect_gazepoint_blinks}
\title{Detect blink intervals from pupil measurements}
\usage{
detect_gazepoint_blinks(
  all_gaze,
  pupil_col = NULL,
  ts_col = "TIME",
  id_col = "USER_ID",
  group_cols = NULL,
  min_duration = 50,
  z_thresh = 4,
  zero_threshold = 0,
  merge_gap_ms = 20,
  time_unit = c("auto", "seconds", "milliseconds"),
  include_rapid_changes = TRUE,
  return = c("events", "samples", "both")
)
}
\arguments{
\item{all_gaze}{A data frame containing sample-level pupil data.}

\item{pupil_col}{Pupil column or columns. When \code{NULL}, common gp3tools and
Gazepoint pupil names are detected automatically.}

\item{ts_col}{Timestamp column.}

\item{id_col}{Participant identifier column.}

\item{group_cols}{Optional additional grouping columns.}

\item{min_duration}{Minimum retained blink duration in milliseconds.}

\item{z_thresh}{Robust threshold for low values and rapid changes.}

\item{zero_threshold}{Values at or below this threshold are invalid.}

\item{merge_gap_ms}{Merge blink candidates separated by no more than this
duration.}

\item{time_unit}{Timestamp unit.}

\item{include_rapid_changes}{Include robust rapid drop and recovery flags.}

\item{return}{Return event intervals, sample labels, or both.}
}
\value{
A blink-event tibble, a labelled sample table, or both.
}
\description{
Detects blink-like periods using missing or non-positive pupil samples and
optional robust drop/recovery rules. Multiple pupil columns can be supplied;
their row-wise mean is used for detection.
}
\examples{
pupil <- data.frame(
  USER_ID = "P01",
  TIME = seq(0, 0.19, by = 0.01),
  mean_pupil = c(rep(3.2, 7), NA, NA, NA, rep(3.2, 10))
)
detect_gazepoint_blinks(pupil, min_duration = 20)
}
