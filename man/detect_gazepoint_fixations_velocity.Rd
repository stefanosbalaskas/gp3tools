% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_detection_extensions.R
\name{detect_gazepoint_fixations_velocity}
\alias{detect_gazepoint_fixations_velocity}
\title{Detect fixations with a velocity-threshold algorithm}
\usage{
detect_gazepoint_fixations_velocity(
  all_gaze,
  id_col = "USER_ID",
  x_col = "FPOGX",
  y_col = "FPOGY",
  ts_col = "TIME",
  vmax = 10,
  min_duration = 50,
  group_cols = NULL,
  time_unit = c("auto", "seconds", "milliseconds"),
  x_scale = 1,
  y_scale = 1,
  return = c("events", "samples", "both"),
  keep_single_sample = FALSE
)
}
\arguments{
\item{all_gaze}{A data frame containing sample-level gaze data.}

\item{id_col}{Participant identifier column.}

\item{x_col}{Horizontal gaze-coordinate column.}

\item{y_col}{Vertical gaze-coordinate column.}

\item{ts_col}{Timestamp column.}

\item{vmax}{Maximum velocity classified as fixation. The threshold is in
scaled coordinate units per second.}

\item{min_duration}{Minimum fixation duration in milliseconds.}

\item{group_cols}{Optional additional grouping columns, such as stimulus or
trial identifiers.}

\item{time_unit}{Timestamp unit. \code{"auto"} infers seconds versus milliseconds
from positive timestamp differences.}

\item{x_scale, y_scale}{Multipliers applied to coordinate differences before
velocity is calculated. Use these to convert native coordinates to visual
degrees when an appropriate conversion is available.}

\item{return}{Return fixation \code{"events"}, sample labels, or \code{"both"}.}

\item{keep_single_sample}{Retain single-sample events when they satisfy
\code{min_duration}.}
}
\value{
A tibble of fixation events, a labelled sample table, or a list
containing both.
}
\description{
Converts sample-level gaze coordinates into fixation events using an
I-VT-style velocity threshold. The function is intended as a high-level
event-table companion to \code{\link[=detect_gazepoint_fixations_ivt]{detect_gazepoint_fixations_ivt()}}.
}
\examples{
gaze <- data.frame(
  USER_ID = "P01",
  TIME = seq(0, 0.19, by = 0.01),
  FPOGX = c(rep(0.25, 10), rep(0.75, 10)),
  FPOGY = 0.50
)
detect_gazepoint_fixations_velocity(
  gaze,
  vmax = 5,
  min_duration = 40
)
}
