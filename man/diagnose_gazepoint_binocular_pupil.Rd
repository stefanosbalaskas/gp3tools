% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binocular_pupil_reconstruction.R
\name{diagnose_gazepoint_binocular_pupil}
\alias{diagnose_gazepoint_binocular_pupil}
\title{Diagnose binocular pupil quality and agreement}
\usage{
diagnose_gazepoint_binocular_pupil(
  data,
  left_col,
  right_col,
  time_col = NULL,
  group_cols = NULL,
  time_unit = c("auto", "milliseconds", "seconds"),
  valid_min = NULL,
  valid_max = NULL,
  min_pairs = 30L,
  min_unique = 5L,
  disagreement_mad_k = 6
)
}
\arguments{
\item{data}{A data frame containing left and right pupil measurements.}

\item{left_col, right_col}{Names of numeric pupil columns.}

\item{time_col}{Optional numeric time column. When supplied, gap durations and
time-order diagnostics are reported.}

\item{group_cols}{Optional grouping columns, typically participant and/or session.}

\item{time_unit}{Unit for \code{time_col}: \code{"auto"}, \code{"milliseconds"}, or \code{"seconds"}.}

\item{valid_min, valid_max}{Optional physiological or instrument-specific bounds.
Values outside the declared bounds are treated as unavailable for diagnostics;
the source data remain unchanged.}

\item{min_pairs}{Minimum bilateral observations used to label a group as
calibration-eligible.}

\item{min_unique}{Minimum unique values required in each eye for eligibility.}

\item{disagreement_mad_k}{Robust multiplier used to describe unusually large
absolute between-eye differences. This is a diagnostic flag, not an exclusion
threshold.}
}
\value{
An object of class \code{gp3_binocular_diagnostics} containing \code{summary},
\code{gaps}, and \code{settings} components.
}
\description{
Quantifies binocular availability, monocular loss, left/right agreement,
systematic offset, regression diagnostics, and temporal missingness runs before
any model-based reconstruction is attempted. Source pupil columns are never
modified.
}
\details{
The Bland-Altman-style limits returned here are descriptive
mean-difference +/- 1.96 SD limits. They do not establish interchangeability
of the two measurements. \code{correlation} is Pearson correlation and
\code{rank_correlation} is Spearman correlation on bilateral observations.
}
\examples{
dat <- simulate_gazepoint_pupil_data(n_subjects = 4, n_trials = 2, seed = 11)
dat$pupil_left[25:28] <- NA_real_
diagnose_gazepoint_binocular_pupil(
  dat, "pupil_left", "pupil_right",
  time_col = "timestamp_ms", group_cols = "subject", min_pairs = 20
)

}
\seealso{
\code{\link[=fit_gazepoint_binocular_calibration]{fit_gazepoint_binocular_calibration()}},
\code{\link[=reconstruct_gazepoint_binocular_pupil]{reconstruct_gazepoint_binocular_pupil()}}
}
