% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil_signal_extensions.R
\name{downsample_gazepoint_pupil}
\alias{downsample_gazepoint_pupil}
\title{Downsample pupil data by integer-factor aggregation}
\usage{
downsample_gazepoint_pupil(
  master_df,
  factor = 2,
  pupil_cols = NULL,
  id_col = "USER_ID",
  group_cols = NULL,
  ts_col = "TIME",
  method = c("mean", "first"),
  keep_bin = FALSE
)
}
\arguments{
\item{master_df}{A sample-level data frame.}

\item{factor}{Positive integer downsampling factor.}

\item{pupil_cols}{Pupil columns to aggregate. When \code{NULL}, common pupil
columns are detected automatically.}

\item{id_col}{Participant identifier.}

\item{group_cols}{Optional additional independent-sequence columns.}

\item{ts_col}{Optional timestamp column. Its bin value is the finite mean.}

\item{method}{\code{"mean"} aggregation or \code{"first"}-sample decimation.}

\item{keep_bin}{Keep the generated downsample-bin identifier.}
}
\value{
A downsampled data frame.
}
\description{
Aggregates consecutive samples within independent sequences. The default
mean method is safer than simple decimation because it reduces aliasing and
preserves a representative timestamp for each bin.
}
\examples{
pupil <- data.frame(
  USER_ID = "P01",
  TIME = seq(0, 0.09, by = 0.01),
  mean_pupil = seq(3, 4, length.out = 10)
)
downsample_gazepoint_pupil(pupil, factor = 2)
}
