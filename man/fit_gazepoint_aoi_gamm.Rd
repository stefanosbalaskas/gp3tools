% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_gazepoint_aoi_gamm.R
\name{fit_gazepoint_aoi_gamm}
\alias{fit_gazepoint_aoi_gamm}
\title{Fit AOI time-course GAMMs}
\usage{
fit_gazepoint_aoi_gamm(
  data,
  include_condition = TRUE,
  condition_smooths = TRUE,
  random_subject = TRUE,
  random_subject_time = FALSE,
  time_k = 10,
  subject_time_k = 5,
  family = stats::binomial(),
  method = "fREML",
  discrete = FALSE,
  select = FALSE,
  drop_non_ok = TRUE,
  min_rows = 10,
  min_subjects = 2,
  min_time_bins = 4,
  ...
)
}
\arguments{
\item{data}{A data frame returned by \code{prepare_gazepoint_aoi_gamm_data()}.}

\item{include_condition}{Logical. If \code{TRUE}, include condition as a
parametric fixed effect when two or more conditions are available.}

\item{condition_smooths}{Logical. If \code{TRUE}, fit condition-specific time
smooths when two or more conditions are available.}

\item{random_subject}{Logical. If \code{TRUE}, include a subject random-effect
smooth.}

\item{random_subject_time}{Logical. If \code{TRUE}, include subject-specific
factor-smooth time deviations. This can be useful for repeated-measures
time-course data but may be too heavy for very small datasets.}

\item{time_k}{Basis dimension for the main time smooth.}

\item{subject_time_k}{Basis dimension for subject-specific factor-smooth
time deviations.}

\item{family}{Model family. Defaults to \code{stats::binomial()}.}

\item{method}{Smoothing-parameter estimation method passed to
\code{mgcv::bam()}.}

\item{discrete}{Logical passed to \code{mgcv::bam()}.}

\item{select}{Logical passed to \code{mgcv::bam()}.}

\item{drop_non_ok}{Logical. If \code{TRUE}, keep only rows with
\code{.gp3_aoi_gamm_status == "ok"} before fitting.}

\item{min_rows}{Minimum number of rows required for model fitting.}

\item{min_subjects}{Minimum number of subjects required for model fitting.}

\item{min_time_bins}{Minimum number of time bins required for model fitting.}

\item{...}{Additional arguments passed to \code{mgcv::bam()}.}
}
\value{
A list containing the fitted model, formula, model status,
diagnostics, parametric table, smooth table, and settings.
}
\description{
Fit binomial GAMMs for AOI target-looking time courses prepared by
\code{prepare_gazepoint_aoi_gamm_data()}. The model uses target-looking
successes and failures over time and can include condition effects,
condition-specific smooths, and subject random-effect smooths.
}
\details{
This function is intended for AOI time-course modelling. It is separate from
confirmatory AOI-window GLMMs and from cluster-based permutation tests.
}
