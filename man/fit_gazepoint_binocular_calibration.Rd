% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binocular_pupil_reconstruction.R
\name{fit_gazepoint_binocular_calibration}
\alias{fit_gazepoint_binocular_calibration}
\title{Fit audited cross-eye pupil calibration models}
\usage{
fit_gazepoint_binocular_calibration(
  data,
  left_col,
  right_col,
  group_cols = NULL,
  fallback_group_cols = NULL,
  valid_min = NULL,
  valid_max = NULL,
  min_pairs = 30L,
  min_unique = 5L,
  min_r2 = NULL,
  allow_negative_slope = FALSE,
  max_abs_slope = NULL
)
}
\arguments{
\item{data}{A data frame containing pupil channels.}

\item{left_col, right_col}{Numeric pupil columns.}

\item{group_cols}{Primary calibration grouping columns.}

\item{fallback_group_cols}{Optional list of fallback grouping specifications.
By default a pooled fallback is added when \code{group_cols} is non-empty. Use
\code{list()} to disable fallback.}

\item{valid_min, valid_max}{Optional measurement bounds.}

\item{min_pairs}{Minimum bilateral training observations per model.}

\item{min_unique}{Minimum unique predictor and outcome values.}

\item{min_r2}{Optional minimum in-sample R-squared. \code{NULL} reports R-squared
without using it as a gate.}

\item{allow_negative_slope}{Whether a non-positive cross-eye slope can be
eligible. The conservative default is \code{FALSE}.}

\item{max_abs_slope}{Optional absolute slope ceiling; use \code{NULL} for none.}
}
\value{
An object of class \code{gp3_binocular_calibration} with a flat \code{models}
table, per-level model tables, and settings.
}
\description{
Fits separate left-from-right and right-from-left linear calibration models
using only bilateral observations. Optional fallback levels permit transparent
participant/session-to-pooled fallback without silently mixing calibration
scopes.
}
\details{
R-squared is a diagnostic, not evidence that reconstructed values are
measured observations. Calibration eligibility is explicit and can be
reviewed before reconstruction.
}
\examples{
dat <- simulate_gazepoint_pupil_data(n_subjects = 4, n_trials = 2, seed = 12)
fit_gazepoint_binocular_calibration(
  dat, "pupil_left", "pupil_right", group_cols = "subject", min_pairs = 20
)

}
\references{
Ong J, He W, Maglanque P, Jiang X, Gillman LM, Vergis A,
Hardy K (2025). A Preprocessing Pipeline for Pupillometry Signal from
Multimodal iMotion Data. \emph{Sensors}, 25(15), 4737.
\doi{10.3390/s25154737}
}
\seealso{
\code{\link[=validate_gazepoint_binocular_reconstruction]{validate_gazepoint_binocular_reconstruction()}},
\code{\link[=reconstruct_gazepoint_binocular_pupil]{reconstruct_gazepoint_binocular_pupil()}}
}
