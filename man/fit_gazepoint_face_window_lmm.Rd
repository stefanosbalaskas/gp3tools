% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multimodal_modelling.R
\name{fit_gazepoint_face_window_lmm}
\alias{fit_gazepoint_face_window_lmm}
\title{Fit a facial-behaviour window mixed or linear model}
\usage{
fit_gazepoint_face_window_lmm(
  data,
  outcome,
  predictors,
  covariates = NULL,
  random_effects = NULL,
  na_action = c("na.omit", "na.exclude"),
  REML = FALSE
)
}
\arguments{
\item{data}{A face-window or multimodal data frame.}

\item{outcome}{Outcome column name.}

\item{predictors}{Character vector of fixed-effect predictor columns.}

\item{covariates}{Optional character vector of covariate columns.}

\item{random_effects}{Optional random-effects formula component, for example
\code{"(1 | participant_id)"}.}

\item{na_action}{Missing-data handling. One of \code{"na.omit"} or \code{"na.exclude"}.}

\item{REML}{Passed to \code{lme4::lmer()} when a mixed model is fitted.}
}
\value{
A list with model, formula, data, and settings. The object has class
\code{gp3_face_window_lmm}.
}
\description{
Fits an explicit model to a face-window or multimodal analysis table. If
\code{random_effects} is supplied, the model is fitted with \code{lme4::lmer()};
otherwise \code{stats::lm()} is used. The helper is a modelling convenience wrapper
and does not interpret facial-behaviour variables as emotions.
}
\examples{
dat <- data.frame(
  participant_id = c("P001", "P002", "P003"),
  AU12_r_mean = c(0.1, 0.2, 0.3),
  rating = c(3, 4, 5)
)

fit_gazepoint_face_window_lmm(
  dat,
  outcome = "rating",
  predictors = "AU12_r_mean"
)
}
