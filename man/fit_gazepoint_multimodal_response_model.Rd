% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multimodal_modelling.R
\name{fit_gazepoint_multimodal_response_model}
\alias{fit_gazepoint_multimodal_response_model}
\title{Fit a multimodal response model}
\usage{
fit_gazepoint_multimodal_response_model(
  data,
  outcome,
  predictors,
  covariates = NULL,
  random_effects = NULL,
  family = NULL,
  na_action = c("na.omit", "na.exclude"),
  REML = FALSE,
  ...
)
}
\arguments{
\item{data}{A multimodal analysis data frame, usually returned by
\code{prepare_gazepoint_multimodal_data()}.}

\item{outcome}{Outcome column name.}

\item{predictors}{Character vector of fixed-effect predictor columns.}

\item{covariates}{Optional character vector of covariate columns.}

\item{random_effects}{Optional random-effects formula component, for example
\code{"(1 | participant_id)"}.}

\item{family}{Optional model family. If \code{NULL}, \code{stats::lm()} or
\code{lme4::lmer()} is used. If supplied, \code{stats::glm()} or \code{lme4::glmer()} is
used.}

\item{na_action}{Missing-data handling. One of \code{"na.omit"} or \code{"na.exclude"}.}

\item{REML}{Passed to \code{lme4::lmer()} when a linear mixed model is fitted.}

\item{...}{Additional arguments passed to the model-fitting function.}
}
\value{
A list with model, formula, data, and settings. The object has class
\code{gp3_multimodal_response_model}.
}
\description{
Fits an explicit response model using multimodal predictors. Linear models,
generalised linear models, and mixed-effects models are supported depending on
\code{family} and \code{random_effects}. The helper prepares and fits the model only; it
does not make causal, diagnostic, or emotion-inference claims.
}
\examples{
dat <- data.frame(
  participant_id = c("P001", "P002", "P003"),
  AU12_r_mean = c(0.1, 0.2, 0.3),
  dwell_time = c(1.2, 1.4, 1.1),
  rating = c(3, 4, 5)
)

fit_gazepoint_multimodal_response_model(
  dat,
  outcome = "rating",
  predictors = c("AU12_r_mean", "dwell_time")
)
}
