% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_gazepoint_pupil.R
\name{flag_gazepoint_pupil}
\alias{flag_gazepoint_pupil}
\title{Flag invalid, missing, implausible, and outlying Gazepoint pupil samples}
\usage{
flag_gazepoint_pupil(
  master,
  pupil_col = NULL,
  time_col = NULL,
  missing_pupil_col = NULL,
  group_cols = c("subject", "media_id"),
  min_pupil = 0,
  max_pupil = Inf,
  outlier_k = 1.5,
  flag_iqr_outliers = TRUE
)
}
\arguments{
\item{master}{A Gazepoint master sample-level table.}

\item{pupil_col}{Optional name of the pupil column to flag. If \code{NULL}, the
function detects one of \code{mean_pupil}, \code{pupil}, \code{pupil_raw}, \code{left_pupil},
or \code{right_pupil}.}

\item{time_col}{Optional name of the time column. If \code{NULL}, the function
detects one of \code{time_ms}, \code{time}, \code{time_orig}, or \code{time_orig_ms}.}

\item{missing_pupil_col}{Optional name of the missing-pupil flag column. If
\code{NULL}, the function uses \code{missing_pupil} when available.}

\item{group_cols}{Character vector of grouping columns used for IQR-based
outlier detection. Defaults to \code{c("subject", "media_id")} using
internally standardised names. Use \code{character(0)} for global outlier
detection.}

\item{min_pupil}{Minimum plausible pupil value. Defaults to \code{0}.}

\item{max_pupil}{Maximum plausible pupil value. Defaults to \code{Inf}. Use
narrower values, such as \code{1} and \code{9}, only when the pupil column is known
to be measured in millimetres.}

\item{outlier_k}{Multiplier for IQR-based outlier detection. Defaults to
\code{1.5}.}

\item{flag_iqr_outliers}{Logical. If \code{TRUE}, IQR-based outliers are flagged.
Defaults to \code{TRUE}.}
}
\value{
A tibble containing the original master table plus pupil-flagging
columns.
}
\description{
Adds pupil-quality flags to a Gazepoint master sample-level table created by
\code{\link[=as_gazepoint_master]{as_gazepoint_master()}} or \code{\link[=create_gazepoint_master]{create_gazepoint_master()}}. This function is
intended as a preprocessing step before interpolation, filtering, baseline
correction, or pupil-based modelling.
}
\examples{
\donttest{
master <- gazepoint_example_master
flagged <- flag_gazepoint_pupil(master)

dplyr::count(flagged, pupil_flag_reason)
}

}
