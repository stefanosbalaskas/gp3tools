% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_gazepoint_pupil_artifacts.R
\name{flag_gazepoint_pupil_artifacts}
\alias{flag_gazepoint_pupil_artifacts}
\title{Flag Gazepoint pupil artifacts before interpolation}
\usage{
flag_gazepoint_pupil_artifacts(
  data,
  pupil_col = NULL,
  left_pupil_col = NULL,
  right_pupil_col = NULL,
  time_col = NULL,
  blink_col = NULL,
  trackloss_col = NULL,
  missing_pupil_col = NULL,
  pupil_unit_col = NULL,
  group_cols = c("subject", "media_id"),
  registry = NULL,
  blink_padding_pre_ms = NULL,
  blink_padding_post_ms = NULL,
  pupil_min_mm = NULL,
  pupil_max_mm = NULL,
  pupil_speed_mad_k = NULL,
  binocular_mad_k = NULL,
  max_physio_outlier_prop = 0.8,
  flag_speed_outliers = TRUE,
  flag_binocular_disagreement = TRUE,
  flag_physiological_outliers = TRUE
)
}
\arguments{
\item{data}{A Gazepoint master table or pupil-processing table.}

\item{pupil_col}{Optional name of the pupil column to clean. If \code{NULL}, the
function detects one of \code{mean_pupil}, \code{pupil_raw}, \code{pupil},
\code{left_pupil}, or \code{right_pupil}.}

\item{left_pupil_col}{Optional left-pupil column. If \code{NULL}, \code{left_pupil} is
used when available.}

\item{right_pupil_col}{Optional right-pupil column. If \code{NULL}, \code{right_pupil}
is used when available.}

\item{time_col}{Optional time column. If \code{NULL}, the function detects one of
\code{time_ms}, \code{time}, \code{time_orig}, or \code{time_orig_ms}.}

\item{blink_col}{Optional blink column. If \code{NULL}, \code{blink} is used when
available.}

\item{trackloss_col}{Optional trackloss column. If \code{NULL}, one of
\code{trackloss} or \code{Trackloss} is used when available.}

\item{missing_pupil_col}{Optional missing-pupil column. If \code{NULL},
\code{missing_pupil} is used when available.}

\item{pupil_unit_col}{Optional pupil-unit column. If \code{NULL}, \code{pupil_unit} is
used when available.}

\item{group_cols}{Character vector of grouping columns used for speed
outlier detection and artifact-padding windows. Defaults to
\code{c("subject", "media_id")}. Use \code{character(0)} for global processing.}

\item{registry}{Optional preprocessing registry created by
\code{\link[=create_gazepoint_preprocessing_registry]{create_gazepoint_preprocessing_registry()}}.}

\item{blink_padding_pre_ms}{Padding before bad samples, in milliseconds. If
\code{NULL}, taken from \code{registry} or defaults to \code{100}.}

\item{blink_padding_post_ms}{Padding after bad samples, in milliseconds. If
\code{NULL}, taken from \code{registry} or defaults to \code{100}.}

\item{pupil_min_mm}{Minimum plausible pupil value when units are millimetres.
If \code{NULL}, taken from \code{registry} or defaults to \code{1}.}

\item{pupil_max_mm}{Maximum plausible pupil value when units are millimetres.
If \code{NULL}, taken from \code{registry} or defaults to \code{9}.}

\item{pupil_speed_mad_k}{MAD multiplier for pupil-speed outlier detection. If
\code{NULL}, taken from \code{registry} or defaults to \code{6}.}

\item{binocular_mad_k}{MAD multiplier for binocular-disagreement detection.
If \code{NULL}, taken from \code{registry} or defaults to \code{6}.}

\item{max_physio_outlier_prop}{Maximum allowed proportion of non-missing
millimetre-labelled pupil samples that may be rejected by the physiological
rule before the rule is automatically suppressed. Defaults to \code{0.80}.
This prevents raw-unit Gazepoint exports from being silently erased when
the unit label suggests millimetres but the numeric scale is not compatible
with ordinary 1--9 mm thresholds.}

\item{flag_speed_outliers}{Logical. If \code{TRUE}, pupil-speed outliers are
flagged. Defaults to \code{TRUE}.}

\item{flag_binocular_disagreement}{Logical. If \code{TRUE}, left-right pupil
disagreement is flagged when both eyes are available. Defaults to \code{TRUE}.}

\item{flag_physiological_outliers}{Logical. If \code{TRUE}, millimetre-based
physiological thresholds are applied only when the pupil unit is identified
as millimetres. Defaults to \code{TRUE}.}
}
\value{
A tibble containing the original data plus pupil-artifact columns.
}
\description{
Adds pupil-specific artifact flags for blink/trackloss contamination,
physiological implausibility when pupil units are millimetres, pupil-speed
outliers, left-right binocular pupil disagreement, and temporal padding
around bad samples. The function preserves raw pupil columns and creates
\code{pupil_clean}, which can be used as input for interpolation.
}
\examples{
\donttest{
master <- gazepoint_example_master
registry <- create_gazepoint_preprocessing_registry()

artifact_pupil <- flag_gazepoint_pupil_artifacts(
  master,
  registry = registry
)

dplyr::count(artifact_pupil, pupil_artifact_reason)
}

}
