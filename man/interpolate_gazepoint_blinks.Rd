% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pupil_signal_extensions.R
\name{interpolate_gazepoint_blinks}
\alias{interpolate_gazepoint_blinks}
\title{Interpolate pupil values across detected blink intervals}
\usage{
interpolate_gazepoint_blinks(
  master_df,
  blink_df,
  pupil_cols = NULL,
  id_col = "USER_ID",
  group_cols = NULL,
  ts_col = "TIME",
  start_col = "start_time",
  end_col = "end_time",
  method = c("linear", "spline"),
  max_gap_ms = 500,
  suffix = "_blink_interp",
  keep_mask = TRUE,
  time_unit = c("auto", "seconds", "milliseconds")
)
}
\arguments{
\item{master_df}{A sample-level data frame.}

\item{blink_df}{Blink intervals returned by \code{\link[=detect_gazepoint_blinks]{detect_gazepoint_blinks()}}.}

\item{pupil_cols}{Pupil columns to interpolate. When \code{NULL}, common pupil
columns are detected automatically.}

\item{id_col}{Participant identifier shared by both inputs.}

\item{group_cols}{Optional additional grouping columns shared by both inputs.}

\item{ts_col}{Timestamp column in \code{master_df}.}

\item{start_col, end_col}{Blink interval boundaries.}

\item{method}{Interpolation method.}

\item{max_gap_ms}{Maximum blink duration eligible for interpolation.}

\item{suffix}{Suffix used for interpolated columns.}

\item{keep_mask}{Add \code{blink_interpolated} and \code{blink_masked} columns.}

\item{time_unit}{Timestamp unit.}
}
\value{
The sample table with interpolated pupil columns.
}
\description{
Masks samples inside blink intervals and interpolates only bounded internal
gaps. Long or edge gaps remain missing.
}
\examples{
pupil <- data.frame(
  USER_ID = "P01",
  TIME = seq(0, 0.09, by = 0.01),
  mean_pupil = c(3, 3.1, 3.2, NA, NA, 3.3, 3.4, 3.5, 3.6, 3.7)
)
blinks <- detect_gazepoint_blinks(
  pupil,
  min_duration = 10
)
interpolate_gazepoint_blinks(pupil, blinks)
}
