% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpolate_gazepoint_pupil_pchip.R
\name{interpolate_gazepoint_pupil_pchip}
\alias{interpolate_gazepoint_pupil_pchip}
\title{Interpolate Gazepoint pupil data using PCHIP}
\usage{
interpolate_gazepoint_pupil_pchip(
  data,
  pupil_col = NULL,
  time_col = NULL,
  grouping_cols = NULL,
  max_gap_ms = 500,
  max_gap_samples = NULL,
  min_valid_points = 3,
  output_col = "pupil_interpolated_pchip",
  flag_col = "interpolated_pupil_pchip",
  status_col = "pchip_interpolation_status"
)
}
\arguments{
\item{data}{A data frame containing pupil time-series data.}

\item{pupil_col}{Name of the pupil column to interpolate. If \code{NULL}, common
processed pupil columns are detected automatically.}

\item{time_col}{Name of the time column. If \code{NULL}, common time columns are
detected automatically.}

\item{grouping_cols}{Optional character vector of grouping columns. If \code{NULL},
common participant/trial/media grouping columns are detected automatically.
Use \code{character(0)} to interpolate the full data as one sequence.}

\item{max_gap_ms}{Maximum internal gap duration, in milliseconds, eligible for
interpolation. If both \code{max_gap_ms} and \code{max_gap_samples} are supplied, both
criteria must be satisfied.}

\item{max_gap_samples}{Maximum number of consecutive missing samples eligible
for interpolation.}

\item{min_valid_points}{Minimum number of valid non-missing points required
within a group before PCHIP interpolation is attempted.}

\item{output_col}{Name of the interpolated pupil output column.}

\item{flag_col}{Name of the logical flag column indicating samples filled by
PCHIP interpolation.}

\item{status_col}{Name of the interpolation-status column.}
}
\value{
A tibble with PCHIP interpolation columns added.
}
\description{
Apply shape-preserving piecewise cubic Hermite interpolation to short internal
gaps in Gazepoint pupil time series. This helper is intended as a sensitivity
branch alongside the default linear interpolation workflow. It does not
overwrite the original pupil column.
}
