% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap_spatial_visualisation.R
\name{plot_gazepoint_heatmap}
\alias{plot_gazepoint_heatmap}
\title{Plot a Gazepoint gaze or fixation heatmap}
\usage{
plot_gazepoint_heatmap(
  data,
  x_col = NULL,
  y_col = NULL,
  weight_col = NULL,
  display_width = NULL,
  display_height = NULL,
  coordinate_space = c("auto", "normalized", "pixel"),
  bins = 60,
  alpha = 0.85,
  normalize = TRUE,
  show_points = FALSE,
  show_legend = TRUE
)
}
\arguments{
\item{data}{A data frame or an object returned by
\code{prepare_gazepoint_heatmap_data()}.}

\item{x_col, y_col}{Character strings giving x and y coordinate columns. These
may be omitted when \code{data} is already prepared by
\code{prepare_gazepoint_heatmap_data()}.}

\item{weight_col}{Optional non-negative weight column, such as fixation
duration.}

\item{display_width, display_height}{Display width and height in pixels.}

\item{coordinate_space}{One of \code{"auto"}, \code{"normalized"}, or \code{"pixel"}.}

\item{bins}{Number of bins. Either one integer or two integers for x and y.}

\item{alpha}{Heatmap layer transparency.}

\item{normalize}{Logical. If \code{TRUE}, bin intensities are scaled to the range
0--1.}

\item{show_points}{Logical. If \code{TRUE}, raw points are added over the heatmap.}

\item{show_legend}{Logical. If \code{TRUE}, the fill legend is shown.}
}
\value{
A ggplot object.
}
\description{
\code{plot_gazepoint_heatmap()} creates a binned spatial heatmap from gaze or
fixation coordinates. Points may optionally be weighted by duration.
}
\examples{
gaze <- data.frame(
  x = c(0.20, 0.25, 0.27, 0.70, 0.75),
  y = c(0.30, 0.32, 0.34, 0.55, 0.60),
  duration = c(120, 180, 160, 90, 100)
)

plot_gazepoint_heatmap(
  gaze,
  x_col = "x",
  y_col = "y",
  weight_col = "duration",
  display_width = 1920,
  display_height = 1080,
  bins = 20
)
}
