% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap_spatial_visualisation.R
\name{plot_gazepoint_heatmap_overlay}
\alias{plot_gazepoint_heatmap_overlay}
\title{Plot a Gazepoint heatmap over a background image}
\usage{
plot_gazepoint_heatmap_overlay(
  data,
  background_image,
  x_col = NULL,
  y_col = NULL,
  weight_col = NULL,
  display_width = NULL,
  display_height = NULL,
  coordinate_space = c("auto", "normalized", "pixel"),
  bins = 60,
  heatmap_alpha = 0.7,
  background_alpha = 1,
  normalize = TRUE,
  show_legend = TRUE
)
}
\arguments{
\item{data}{A data frame or prepared heatmap data.}

\item{background_image}{Path to a PNG background image.}

\item{x_col, y_col}{Character strings giving x and y coordinate columns.}

\item{weight_col}{Optional non-negative weight column.}

\item{display_width, display_height}{Display width and height in pixels. If
omitted, the PNG image dimensions are used.}

\item{coordinate_space}{One of \code{"auto"}, \code{"normalized"}, or \code{"pixel"}.}

\item{bins}{Number of heatmap bins. Either one integer or two integers.}

\item{heatmap_alpha}{Heatmap layer transparency.}

\item{background_alpha}{Background image transparency.}

\item{normalize}{Logical. If \code{TRUE}, bin intensities are scaled to 0--1.}

\item{show_legend}{Logical. If \code{TRUE}, the fill legend is shown.}
}
\value{
A ggplot object.
}
\description{
\code{plot_gazepoint_heatmap_overlay()} overlays a binned gaze or fixation
heatmap on a PNG background image, such as a stimulus screenshot. The \code{png}
package is used only when this helper is called.
}
\examples{
gaze <- data.frame(
  x = c(0.20, 0.25, 0.70),
  y = c(0.30, 0.35, 0.60),
  duration = c(120, 200, 80)
)

if (requireNamespace("png", quietly = TRUE)) {
  bg <- tempfile(fileext = ".png")
  img <- array(1, dim = c(200, 300, 3))
  png::writePNG(img, bg)

  plot_gazepoint_heatmap_overlay(
    gaze,
    background_image = bg,
    x_col = "x",
    y_col = "y",
    weight_col = "duration"
  )
}
}
