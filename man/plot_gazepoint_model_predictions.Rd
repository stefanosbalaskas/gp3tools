% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gazepoint_model_predictions.R
\name{plot_gazepoint_model_predictions}
\alias{plot_gazepoint_model_predictions}
\title{Plot observed summaries and model-implied predictions}
\usage{
plot_gazepoint_model_predictions(
  data,
  model = NULL,
  x_col,
  outcome_col,
  condition_col = NULL,
  group_cols = NULL,
  facet_cols = NULL,
  newdata = NULL,
  prediction_type = c("response", "link"),
  include_random_effects = FALSE,
  observed_summary_function = c("mean", "median"),
  ci = 0.95,
  show_observed = TRUE,
  show_observed_ci = TRUE,
  show_predictions = TRUE,
  show_prediction_ci = TRUE,
  point_alpha = 0.55,
  line_width = 1,
  name = "gazepoint_model_predictions"
)
}
\arguments{
\item{data}{A data frame containing the observed data.}

\item{model}{Optional fitted model object. If supplied, predictions are
computed using \code{predict()}.}

\item{x_col}{Column used on the x-axis, usually time or time bin.}

\item{outcome_col}{Observed outcome column.}

\item{condition_col}{Optional condition column used for colour/grouping.}

\item{group_cols}{Optional additional grouping columns for observed and
predicted trajectories.}

\item{facet_cols}{Optional columns used for faceting.}

\item{newdata}{Optional prediction grid. If \code{NULL}, predictions are computed
on \code{data} and then summarised by x/group/facet.}

\item{prediction_type}{Prediction scale passed to \code{predict()}. Common values
are \code{"response"} and \code{"link"}.}

\item{include_random_effects}{Logical. For \code{lme4} mixed models, \code{FALSE}
requests population-level predictions via \code{re.form = NA}; \code{TRUE} includes
conditional random effects where possible.}

\item{observed_summary_function}{Summary for observed outcomes. Options are
\code{"mean"} and \code{"median"}.}

\item{ci}{Confidence level for observed and prediction intervals when
standard errors are available.}

\item{show_observed}{Logical. Plot observed summaries.}

\item{show_observed_ci}{Logical. Plot observed normal-approximation intervals.}

\item{show_predictions}{Logical. Plot model predictions when \code{model} is
supplied.}

\item{show_prediction_ci}{Logical. Plot prediction intervals when standard
errors are available from \code{predict()}.}

\item{point_alpha}{Alpha value for observed points.}

\item{line_width}{Line width for prediction trajectories.}

\item{name}{Character label stored in plot attributes.}
}
\value{
A \code{ggplot} object with attributes containing the observed summary,
prediction summary, overview, and settings.
}
\description{
Create a reporting plot that overlays observed outcome summaries with
fitted/model-predicted trajectories. The helper is intentionally generic and
can be used with linear models, GLMs, mixed models, GAMMs, GCA-style models,
AOI GLMMs, and pupil LMMs when the fitted object supports \code{predict()}.
}
