% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase_segmentation_extensions.R
\name{plot_gazepoint_phase_timeline}
\alias{plot_gazepoint_phase_timeline}
\title{Plot a Gazepoint task-phase timeline}
\usage{
plot_gazepoint_phase_timeline(
  data,
  phase_col = "task_phase",
  group_cols = NULL,
  time_col = NULL,
  title = NULL
)
}
\arguments{
\item{data}{A segmented data frame or a summary produced by
\code{summarize_gazepoint_phase_coverage()}.}

\item{phase_col}{Character name of the phase column when \code{data} is raw data.}

\item{group_cols}{Optional grouping columns when \code{data} is raw data.}

\item{time_col}{Optional time column when \code{data} is raw data.}

\item{title}{Optional plot title.}
}
\value{
A ggplot object.
}
\description{
Creates a descriptive timeline plot from segmented Gazepoint-style data or
from a phase-coverage summary. The plot is intended for visual inspection of
task-phase coverage and timing, not for inferential analysis.
}
\examples{
x <- data.frame(time_ms = c(0, 250, 750, 1250))
windows <- data.frame(
  phase = c("baseline", "stimulus"),
  start = c(0, 500),
  end = c(500, 1500)
)
segmented <- segment_gazepoint_task_phases(x, "time_ms", windows)
plot_gazepoint_phase_timeline(segmented, time_col = "time_ms")
}
