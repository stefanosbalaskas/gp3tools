% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scanpath_cluster_stability.R
\name{plot_gazepoint_scanpath_cluster_stability}
\alias{plot_gazepoint_scanpath_cluster_stability}
\title{Plot scanpath-cluster stability}
\usage{
plot_gazepoint_scanpath_cluster_stability(
  x,
  plot = c("coclustering", "ari", "sequence"),
  specification = NULL,
  min_pair_coverage = 0.5,
  stable_threshold = 0.75,
  main = NULL,
  label_cex = 0.8
)
}
\arguments{
\item{x}{An object returned by
\code{\link[=bootstrap_gazepoint_scanpath_clusters]{bootstrap_gazepoint_scanpath_clusters()}}.}

\item{plot}{Plot type: \code{"coclustering"}, \code{"ari"}, or \code{"sequence"}.}

\item{specification}{Optional specification name. The first specification
is used by default.}

\item{min_pair_coverage}{Pair-coverage threshold used in sequence summaries.}

\item{stable_threshold}{Stability threshold used in sequence summaries.}

\item{main}{Optional plot title.}

\item{label_cex}{Axis-label size multiplier.}
}
\value{
Invisibly returns a list containing the plot type,
specification, and plotted data.
}
\description{
Create base-R diagnostics for co-clustering probabilities,
iteration-level adjusted Rand indices, or sequence-level stability.
}
\examples{
latent <- rep(1:3, each = 2)
d <- outer(
  latent,
  latent,
  FUN = function(x, y) ifelse(x == y, 0.1, 1)
)
diag(d) <- 0
dimnames(d) <- list(LETTERS[1:6], LETTERS[1:6])

stability <- bootstrap_gazepoint_scanpath_clusters(
  d,
  k = 3,
  n_boot = 10,
  seed = 1
)

plot_gazepoint_scanpath_cluster_stability(
  stability,
  plot = "coclustering"
)
}
