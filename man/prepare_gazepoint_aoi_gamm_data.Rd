% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_gazepoint_aoi_gamm_data.R
\name{prepare_gazepoint_aoi_gamm_data}
\alias{prepare_gazepoint_aoi_gamm_data}
\title{Prepare AOI time-course data for GAMM analysis}
\usage{
prepare_gazepoint_aoi_gamm_data(
  data,
  aoi_col = "aoi_current",
  target_aoi_values = NULL,
  outcome_col = NULL,
  subject_col = "subject",
  condition_col = "condition",
  time_col = "time",
  trial_col = NULL,
  time_bin_col = NULL,
  conditions = NULL,
  time_window = NULL,
  bin_size_ms = 50,
  denominator = c("valid", "all", "aoi_only"),
  valid_aoi_values = NULL,
  non_aoi_values = c("non_aoi"),
  missing_aoi_values = c("missing", ""),
  min_denominator_samples = 1,
  drop_invalid = TRUE,
  missing_condition_label = "all_data",
  outcome_label = "target_aoi"
)
}
\arguments{
\item{data}{A data frame containing sample-level or binned AOI data.}

\item{aoi_col}{Name of the AOI-state column. Used when \code{outcome_col = NULL}.}

\item{target_aoi_values}{Character vector identifying target AOI values.
Required when \code{outcome_col = NULL}.}

\item{outcome_col}{Optional logical or 0/1 numeric column indicating target
AOI looking at the sample level. If supplied, this takes priority over
\code{aoi_col} and \code{target_aoi_values}.}

\item{subject_col}{Name of the subject/participant column.}

\item{condition_col}{Name of the condition column. If unavailable, a single
fallback condition is created.}

\item{time_col}{Name of the time column, in milliseconds.}

\item{trial_col}{Optional trial identifier column.}

\item{time_bin_col}{Optional existing time-bin column. If \code{NULL}, time bins
are created from \code{time_col} using \code{bin_size_ms}.}

\item{conditions}{Optional character vector of condition levels to retain
and order.}

\item{time_window}{Optional numeric vector of length 2 defining the retained
time window in milliseconds.}

\item{bin_size_ms}{Time-bin width in milliseconds when \code{time_bin_col = NULL}.}

\item{denominator}{Denominator definition. \code{"valid"} uses non-missing AOI
states, \code{"all"} uses all retained rows, and \code{"aoi_only"} uses only explicit
AOI states.}

\item{valid_aoi_values}{Optional character vector defining explicit AOI
values for \code{"aoi_only"} denominators. If \code{NULL}, values beginning with
\code{"AOI"} are treated as explicit AOIs, excluding \code{non_aoi_values}.}

\item{non_aoi_values}{Character vector identifying non-AOI/background states.}

\item{missing_aoi_values}{Character vector identifying missing AOI-state
labels.}

\item{min_denominator_samples}{Minimum number of denominator samples required
per subject-condition-time bin.}

\item{drop_invalid}{Logical. If \code{TRUE}, bins with zero or low denominators
are removed from the returned data.}

\item{missing_condition_label}{Fallback condition label when no usable
condition column is available.}

\item{outcome_label}{Descriptive label for the AOI-GAMM outcome.}
}
\value{
A tibble with standardised AOI-GAMM columns.
}
\description{
Prepare sample-level or binned Gazepoint AOI data for AOI time-course GAMM
analysis. The function creates binned subject-by-condition-by-time summaries
with binomial success/failure counts for target-AOI looking over time.
}
\details{
This helper is intended for modelling AOI time-course trajectories, such as
target-AOI looking probability over time. It is separate from confirmatory
AOI-window GLMMs and from cluster-based permutation tests.
}
