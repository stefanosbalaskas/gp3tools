% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_gazepoint_aoi_glmm_data.R
\name{prepare_gazepoint_aoi_glmm_data}
\alias{prepare_gazepoint_aoi_glmm_data}
\title{Prepare AOI-window data for binomial GLMMs}
\usage{
prepare_gazepoint_aoi_glmm_data(
  data,
  success_col = "n_target_samples",
  denominator = c("valid", "all", "aoi", "custom"),
  denominator_col = NULL,
  valid_denominator_col = "n_valid_denominator_samples",
  all_denominator_col = "n_window_samples",
  aoi_denominator_col = "n_aoi_samples",
  subject_col = "subject",
  condition_col = "condition",
  window_col = "window_label",
  window_start_col = "window_start_ms",
  window_end_col = "window_end_ms",
  group_cols = NULL,
  min_denominator_samples = 1,
  drop_invalid = TRUE,
  missing_condition_label = "all_data",
  outcome_label = "target"
)
}
\arguments{
\item{data}{AOI-window summary data.}

\item{success_col}{Column containing the success count. For target-looking
models this is usually \code{n_target_samples}.}

\item{denominator}{Denominator definition. Use \code{"valid"} for valid AOI-window
denominator samples, \code{"all"} for all window samples, \code{"aoi"} for AOI-only
samples, or \code{"custom"} with \code{denominator_col}.}

\item{denominator_col}{Custom denominator column when \code{denominator = "custom"}.}

\item{valid_denominator_col}{Column used when \code{denominator = "valid"}.}

\item{all_denominator_col}{Column used when \code{denominator = "all"}.}

\item{aoi_denominator_col}{Column used when \code{denominator = "aoi"}.}

\item{subject_col}{Subject/participant column.}

\item{condition_col}{Optional condition column.}

\item{window_col}{AOI-window label column.}

\item{window_start_col}{Optional window-start column.}

\item{window_end_col}{Optional window-end column.}

\item{group_cols}{Optional extra grouping columns to keep/check.}

\item{min_denominator_samples}{Minimum acceptable denominator.}

\item{drop_invalid}{Logical. If \code{TRUE}, rows with invalid binomial counts or
too-small denominators are removed.}

\item{missing_condition_label}{Label used when condition is missing.}

\item{outcome_label}{Label stored in the output to identify the modelled AOI
outcome.}
}
\value{
A tibble of GLMM-ready AOI-window rows.
}
\description{
Prepare AOI-window summaries for confirmatory binomial mixed-effects
modelling. The function creates success, failure, denominator, proportion,
subject, condition, and window columns from output produced by
\code{summarise_gazepoint_aoi_windows()}.
}
