% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_gazepoint_aoi_sequences.R
\name{prepare_gazepoint_aoi_sequences}
\alias{prepare_gazepoint_aoi_sequences}
\title{Prepare Gazepoint AOI sequences}
\usage{
prepare_gazepoint_aoi_sequences(
  data,
  aoi_col = NULL,
  time_col = "time",
  group_cols = c("subject", "MEDIA_ID", "trial_global"),
  include_non_aoi = TRUE,
  non_aoi_values = c("non_aoi", "none", "background", "outside", "outside_aoi",
    "missing", "missing_aoi"),
  missing_aoi_label = "missing_aoi",
  include_terminal = TRUE
)
}
\arguments{
\item{data}{A Gazepoint sample-level data frame or an AOI-entry table created
by \code{summarise_gazepoint_aoi_entries()}.}

\item{aoi_col}{Name of the AOI-state column. Used only when \code{data} is
sample-level data. If \code{NULL}, the function tries \code{aoi_current}, \code{AOI},
and \code{aoi_state}.}

\item{time_col}{Name of the time column, in milliseconds. Used only when
\code{data} is sample-level data.}

\item{group_cols}{Character vector of columns defining independent AOI
sequences, usually subject/media/trial.}

\item{include_non_aoi}{Logical. If \code{TRUE}, non-AOI/background states are
retained. If \code{FALSE}, non-AOI/background states are removed before sequence
and transition fields are computed.}

\item{non_aoi_values}{Character vector of AOI labels treated as background
or non-AOI states.}

\item{missing_aoi_label}{Label used when the AOI value is missing.}

\item{include_terminal}{Logical. If \code{TRUE}, the final state of each sequence
is retained with \code{next_state = NA}. If \code{FALSE}, terminal states are removed
so that each output row represents an observed transition.}
}
\value{
A tibble with ordered AOI sequence and transition fields.
}
\description{
Create ordered AOI-state sequences from sample-level Gazepoint AOI data or
from the output of \code{summarise_gazepoint_aoi_entries()}. The output is
transition-ready and includes the current AOI state, previous state, next
state, dwell time before transition, and self-transition flags.
}
