% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_gazepoint_cluster_data.R
\name{prepare_gazepoint_cluster_data}
\alias{prepare_gazepoint_cluster_data}
\title{Prepare time-course data for cluster-based permutation tests}
\usage{
prepare_gazepoint_cluster_data(
  data,
  outcome_col,
  subject_col = "subject",
  condition_col = "condition",
  time_col = "time",
  trial_col = NULL,
  time_bin_col = NULL,
  conditions = NULL,
  time_window = NULL,
  bin_size_ms = 50,
  aggregation = c("mean", "proportion", "sum", "median"),
  min_samples_per_bin = 1,
  paired = TRUE,
  drop_invalid = TRUE,
  missing_condition_label = "all_data",
  outcome_label = "outcome"
)
}
\arguments{
\item{data}{A data frame containing sample-level or binned time-course data.}

\item{outcome_col}{Column containing the outcome to test. For AOI analyses
this is often a 0/1 or logical AOI column. For pupil analyses this is
often a processed pupil column.}

\item{subject_col}{Subject/participant column.}

\item{condition_col}{Optional condition column.}

\item{time_col}{Time column in milliseconds.}

\item{trial_col}{Optional trial identifier column.}

\item{time_bin_col}{Optional existing time-bin column. If \code{NULL}, time bins
are created from \code{time_col} and \code{bin_size_ms}.}

\item{conditions}{Optional character vector of condition levels to keep.
Cluster tests are usually pairwise, so this is typically length 2.}

\item{time_window}{Optional numeric vector of length 2 giving the time range
to retain, in milliseconds.}

\item{bin_size_ms}{Bin size in milliseconds when \code{time_bin_col = NULL}.}

\item{aggregation}{How to aggregate samples within subject-condition-time
bins. Supported values are \code{"mean"}, \code{"proportion"}, \code{"sum"}, and
\code{"median"}. \code{"proportion"} is equivalent to the mean of a numeric/logical
0/1 outcome.}

\item{min_samples_per_bin}{Minimum number of samples required per
subject-condition-time bin.}

\item{paired}{Logical. If \code{TRUE}, retain only subjects with all retained
condition levels.}

\item{drop_invalid}{Logical. If \code{TRUE}, rows and bins that are not suitable
for cluster testing are removed.}

\item{missing_condition_label}{Label used when condition is missing or
\code{condition_col} is unavailable.}

\item{outcome_label}{Label stored in the output to identify the outcome.}
}
\value{
A tibble with standardised cluster-test preparation columns.
}
\description{
Prepare sample-level or already binned Gazepoint time-course data for
cluster-based permutation testing. The function standardises subject,
condition, time-bin, outcome, sample-count, trial-count, and status columns.
It can be used for AOI proportions, pupil time-course outcomes, or other
continuous time-varying measures.
}
\details{
Cluster-based permutation tests are intended for time-course inference.
They should not be used to discover a time window and then test that same
window again as a confirmatory analysis.
}
