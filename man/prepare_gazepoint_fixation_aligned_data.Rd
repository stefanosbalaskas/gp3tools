% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_gazepoint_fixation_aligned_data.R
\name{prepare_gazepoint_fixation_aligned_data}
\alias{prepare_gazepoint_fixation_aligned_data}
\title{Prepare fixation- or saccade-contingent aligned Gazepoint data}
\usage{
prepare_gazepoint_fixation_aligned_data(
  data,
  time_col,
  participant_col = NULL,
  trial_col = NULL,
  aoi_col = NULL,
  target_aoi = NULL,
  fixation_col = NULL,
  saccade_col = NULL,
  event_col = NULL,
  event_value = NULL,
  alignment_event = c("first_target_entry", "first_fixation_to_target",
    "first_saccade_to_aoi", "first_fixation", "custom"),
  baseline_window = NULL,
  analysis_window = NULL,
  keep_unaligned = FALSE,
  name = "gazepoint_fixation_aligned_data"
)
}
\arguments{
\item{data}{A data frame containing Gazepoint samples, fixation rows, or
trial-level time-course rows.}

\item{time_col}{Time column.}

\item{participant_col}{Optional participant column.}

\item{trial_col}{Optional trial column.}

\item{aoi_col}{Optional AOI column.}

\item{target_aoi}{Optional character vector identifying the target AOI(s).}

\item{fixation_col}{Optional fixation indicator column.}

\item{saccade_col}{Optional saccade indicator column.}

\item{event_col}{Optional custom event indicator column.}

\item{event_value}{Optional value(s) in \code{event_col} defining the custom event.
If \code{NULL} and \code{alignment_event = "custom"}, \code{event_col} is interpreted as
a logical-like indicator.}

\item{alignment_event}{Alignment event. Options are \code{"first_target_entry"},
\code{"first_fixation_to_target"}, \code{"first_saccade_to_aoi"},
\code{"first_fixation"}, and \code{"custom"}.}

\item{baseline_window}{Optional numeric vector of length two giving the
aligned-time baseline window, for example \code{c(-200, 0)}.}

\item{analysis_window}{Optional numeric vector of length two giving the
aligned-time analysis window, for example \code{c(0, 1000)}.}

\item{keep_unaligned}{Logical. If \code{FALSE}, groups without an alignment event
are removed from \code{aligned_data}. Their status remains in \code{event_table}.}

\item{name}{Character label stored in the returned object.}
}
\value{
A list with class \code{gp3_fixation_aligned_data}.
}
\description{
Align Gazepoint observations to a within-trial event such as first fixation,
first target-AOI entry, first fixation to a target AOI, first saccade to a
target AOI, or a custom event marker. The helper returns the original data
with event-aligned time, event metadata, pre-event/post-event flags, and
trial-level summaries that help separate event-driven looking from looks that
were already present before the event.
}
