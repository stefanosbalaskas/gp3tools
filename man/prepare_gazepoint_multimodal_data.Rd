% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multimodal_modelling.R
\name{prepare_gazepoint_multimodal_data}
\alias{prepare_gazepoint_multimodal_data}
\title{Prepare multimodal Gazepoint and external face-window data}
\usage{
prepare_gazepoint_multimodal_data(
  face_windows,
  gaze_data = NULL,
  response_data = NULL,
  by = NULL,
  gaze_by = NULL,
  response_by = NULL,
  predictor_cols = NULL,
  outcome_cols = NULL,
  covariate_cols = NULL,
  scale_predictors = TRUE,
  scaled_suffix = "_z",
  drop_missing_outcomes = FALSE,
  keep_all = TRUE
)
}
\arguments{
\item{face_windows}{A data frame, usually returned by
\code{summarize_gazepoint_face_windows()} or
\code{summarize_gazepoint_face_reactivity()}.}

\item{gaze_data}{Optional Gazepoint-derived data frame to join.}

\item{response_data}{Optional response/outcome data frame to join.}

\item{by}{Character vector of join columns shared across tables. If \code{NULL},
common identifier-like columns are detected.}

\item{gaze_by}{Optional named join mapping passed to \code{merge()} for
\code{gaze_data}. If \code{NULL}, \code{by} is used.}

\item{response_by}{Optional named join mapping passed to \code{merge()} for
\code{response_data}. If \code{NULL}, \code{by} is used.}

\item{predictor_cols}{Optional predictor columns to mark for modelling.
If \code{NULL}, numeric non-identifier columns from the joined table are used.}

\item{outcome_cols}{Optional outcome columns to mark for modelling.}

\item{covariate_cols}{Optional covariate columns to mark for modelling.}

\item{scale_predictors}{Should numeric predictor columns be z-scaled?}

\item{scaled_suffix}{Suffix for scaled predictor columns.}

\item{drop_missing_outcomes}{Should rows with missing values in any
\code{outcome_cols} be dropped?}

\item{keep_all}{Should all rows from \code{face_windows} be retained during joins?}
}
\value{
A tibble with class \code{gp3_multimodal_data}. Attributes contain join
settings, selected predictors, outcomes, covariates, and scaling metadata.
}
\description{
Joins facial-behaviour window summaries with optional Gazepoint-derived
summaries, response variables, or covariates. The helper is intentionally
conservative: it prepares transparent analysis tables and optional scaled
predictors, but it does not infer emotional states or causal effects.
}
\examples{
face_windows <- data.frame(
  participant_id = c("P001", "P002"),
  trial_id = c(1, 1),
  AU12_r_mean = c(0.2, 0.3),
  face_confidence_mean = c(0.95, 0.94)
)

responses <- data.frame(
  participant_id = c("P001", "P002"),
  trial_id = c(1, 1),
  rating = c(4, 5)
)

prepare_gazepoint_multimodal_data(
  face_windows,
  response_data = responses,
  by = c("participant_id", "trial_id"),
  outcome_cols = "rating",
  predictor_cols = "AU12_r_mean"
)
}
