% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_gazepoint_semimarkov_data.R
\name{prepare_gazepoint_semimarkov_data}
\alias{prepare_gazepoint_semimarkov_data}
\title{Prepare Gazepoint AOI sequences for semi-Markov modelling}
\usage{
prepare_gazepoint_semimarkov_data(
  data,
  state_col = NULL,
  participant_col = NULL,
  trial_col = NULL,
  time_col = NULL,
  duration_col = NULL,
  sequence_id_cols = NULL,
  covariate_cols = NULL,
  exclude_states = c("missing", "missing_aoi", "missing_coordinate", "trackloss",
    "track_loss"),
  missing_state_label = NULL,
  collapse_repeated_states = TRUE,
  include_terminal_states = TRUE,
  terminal_next_state_label = "END",
  name = "gazepoint_semimarkov_data"
)
}
\arguments{
\item{data}{A data frame containing ordered AOI/state observations.}

\item{state_col}{AOI/state column. If \code{NULL}, common AOI/state columns are
detected automatically.}

\item{participant_col}{Optional participant/subject column.}

\item{trial_col}{Optional trial/sequence column.}

\item{time_col}{Optional time/order column.}

\item{duration_col}{Optional sample-duration column. If supplied, dwell
durations are computed by summing this column within each state visit.}

\item{sequence_id_cols}{Optional character vector of columns defining separate
sequences. If \code{NULL}, participant and trial columns are used when available.}

\item{covariate_cols}{Optional character vector of covariate columns to carry
into the state-visit and transition tables using the first value within each
state visit.}

\item{exclude_states}{Character vector of states to exclude before creating
state visits.}

\item{missing_state_label}{Optional label used to retain missing states. If
\code{NULL}, missing/blank states are removed.}

\item{collapse_repeated_states}{Logical. If \code{TRUE}, consecutive repeated
states within a sequence are collapsed into a single dwell episode.}

\item{include_terminal_states}{Logical. If \code{TRUE}, the final state visit in
each sequence is retained as a transition to \code{terminal_next_state_label}.}

\item{terminal_next_state_label}{Label used for the terminal next state.}

\item{name}{Character label stored in the returned object.}
}
\value{
A list with class \code{gp3_semimarkov_data}.
}
\description{
Convert ordered AOI/state observations into state-visit and transition-level
semi-Markov data. Consecutive repeated states can be collapsed into dwell
episodes, producing one row per state visit with dwell duration and next-state
information.
}
