% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_permutation_extensions.R
\name{prepare_gazepoint_timecourse_test_data}
\alias{prepare_gazepoint_timecourse_test_data}
\title{Prepare time-course data for Gazepoint cluster-permutation testing}
\usage{
prepare_gazepoint_timecourse_test_data(
  data,
  subject_col,
  condition_col,
  time_col,
  outcome_col,
  condition_order = NULL,
  aggregate_fun = mean,
  complete_only = TRUE
)
}
\arguments{
\item{data}{A long-format data frame.}

\item{subject_col}{Column identifying participants or paired units.}

\item{condition_col}{Column identifying the two within-subject conditions.}

\item{time_col}{Numeric time or time-bin column.}

\item{outcome_col}{Numeric outcome column.}

\item{condition_order}{Optional character vector of length two giving the
condition order used for contrasts.}

\item{aggregate_fun}{Function used when duplicate subject-condition-time rows
are present. Defaults to \code{mean}.}

\item{complete_only}{If \code{TRUE}, keep only subject-by-time cells with both
conditions present.}
}
\value{
A data frame with internal cluster columns:
\code{.gp3_cluster_subject}, \code{.gp3_cluster_condition},
\code{.gp3_cluster_time_bin}, and \code{.gp3_cluster_outcome}.
}
\description{
\code{prepare_gazepoint_timecourse_test_data()} converts a long-format
subject-by-condition-by-time data frame into the internal column contract used
by \code{run_gazepoint_cluster_permutation()}. It is intended for conservative
two-condition, within-subject, one-dimensional time-course workflows.
}
\examples{
d <- data.frame(
  subject = rep(1:4, each = 6),
  condition = rep(rep(c("A", "B"), each = 3), 4),
  time = rep(1:3, 8),
  value = rnorm(24)
)

prepare_gazepoint_timecourse_test_data(
  d,
  subject_col = "subject",
  condition_col = "condition",
  time_col = "time",
  outcome_col = "value"
)
}
