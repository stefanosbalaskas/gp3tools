% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signal_preprocessing_workflow.R
\name{preprocess_gazepoint_signals}
\alias{preprocess_gazepoint_signals}
\title{Run an integrated Gazepoint signal-preprocessing workflow}
\usage{
preprocess_gazepoint_signals(
  data,
  id_col = "USER_ID",
  group_cols = NULL,
  time_col = "TIME",
  x_col = "FPOGX",
  y_col = "FPOGY",
  left_pupil_col = NULL,
  right_pupil_col = NULL,
  pupil_col = NULL,
  pupil_mode = c("mean", "regression", "none"),
  detect_blinks = TRUE,
  interpolate_blinks = TRUE,
  smooth_pupil = TRUE,
  smooth_coordinates = TRUE,
  downsample_factor = 1L,
  detect_fixations = TRUE,
  blink_args = list(),
  interpolation_args = list(),
  pupil_args = list(),
  pupil_smoothing_args = list(),
  coordinate_smoothing_args = list(),
  downsampling_args = list(),
  fixation_args = list()
)
}
\arguments{
\item{data}{A sample-level Gazepoint or gp3tools data frame.}

\item{id_col}{Participant identifier column.}

\item{group_cols}{Optional additional columns defining independent time
series, such as trial or stimulus.}

\item{time_col}{Timestamp column.}

\item{x_col, y_col}{Gaze-coordinate columns.}

\item{left_pupil_col, right_pupil_col}{Optional binocular pupil columns.
When \code{NULL}, common Gazepoint and gp3tools names are detected.}

\item{pupil_col}{Optional existing monocular or fused pupil column used when
\code{pupil_mode = "none"}.}

\item{pupil_mode}{Binocular fusion mode: \code{"mean"}, \code{"regression"}, or
\code{"none"}.}

\item{detect_blinks}{Should \code{\link[=detect_gazepoint_blinks]{detect_gazepoint_blinks()}} be run?}

\item{interpolate_blinks}{Should detected blink intervals be interpolated?}

\item{smooth_pupil}{Should \code{\link[=smooth_gazepoint_pupil]{smooth_gazepoint_pupil()}} be run?}

\item{smooth_coordinates}{Should \code{\link[=smooth_gazepoint_coordinate]{smooth_gazepoint_coordinate()}} be run?}

\item{downsample_factor}{Positive integer downsampling factor. Use \code{1} to
retain the original sample count.}

\item{detect_fixations}{Should \code{\link[=detect_gazepoint_fixations_velocity]{detect_gazepoint_fixations_velocity()}} be
run on the final full-resolution coordinates?}

\item{blink_args}{Named list overriding blink-detection defaults.}

\item{interpolation_args}{Named list overriding blink-interpolation
defaults.}

\item{pupil_args}{Named list overriding binocular fusion defaults.}

\item{pupil_smoothing_args}{Named list overriding pupil-smoothing defaults.}

\item{coordinate_smoothing_args}{Named list overriding coordinate-smoothing
defaults.}

\item{downsampling_args}{Named list overriding downsampling defaults.}

\item{fixation_args}{Named list overriding fixation-detection defaults.}
}
\value{
An object of class \code{"gp3_signal_preprocessing_result"} containing
processed \code{data}, detected \code{blinks}, detected \code{fixations}, diagnostic
tables, a \code{decision_log}, and resolved \code{settings}.
}
\description{
Orchestrate transparent blink, pupil, coordinate, downsampling, and
velocity-based fixation-processing steps while preserving the original
input columns. Every requested operation is recorded in a decision log.
}
\examples{
pupil <- data.frame(
  USER_ID = rep("P01", 30),
  trial = rep("T01", 30),
  TIME = seq(0, 0.29, by = 0.01),
  FPOGX = c(rep(0.25, 15), rep(0.75, 15)),
  FPOGY = 0.50,
  LPupil = c(rep(3.2, 10), NA, NA, rep(3.2, 18)),
  RPupil = c(rep(3.1, 10), NA, NA, rep(3.1, 18))
)

result <- preprocess_gazepoint_signals(
  pupil,
  group_cols = "trial",
  downsample_factor = 2
)

result$decision_log
}
