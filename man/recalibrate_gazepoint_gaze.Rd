% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recalibrate_gazepoint_gaze.R
\name{recalibrate_gazepoint_gaze}
\alias{recalibrate_gazepoint_gaze}
\title{Offline gaze recalibration using known target coordinates}
\usage{
recalibrate_gazepoint_gaze(
  data,
  x_col,
  y_col,
  target_x_col,
  target_y_col,
  time_col = NULL,
  grouping_cols = NULL,
  calibration_col = NULL,
  calibration_value = NULL,
  method = c("median_shift", "mean_shift"),
  min_valid_points = 3L,
  max_shift = NULL,
  output_x_col = "gaze_x_recalibrated",
  output_y_col = "gaze_y_recalibrated",
  dx_col = "gaze_recalibration_dx",
  dy_col = "gaze_recalibration_dy",
  shift_col = "gaze_recalibration_shift",
  error_before_col = "gaze_error_before_recalibration",
  error_after_col = "gaze_error_after_recalibration",
  status_col = "gaze_recalibration_status",
  overwrite = FALSE,
  name = "gazepoint_gaze_recalibration"
)
}
\arguments{
\item{data}{A data frame containing gaze and target coordinates.}

\item{x_col}{Horizontal gaze coordinate column.}

\item{y_col}{Vertical gaze coordinate column.}

\item{target_x_col}{Known horizontal target coordinate column.}

\item{target_y_col}{Known vertical target coordinate column.}

\item{time_col}{Optional time column used only for stable ordering.}

\item{grouping_cols}{Optional grouping columns used to estimate one correction
per participant, trial, block, stimulus, or other unit.}

\item{calibration_col}{Optional column identifying rows to use for estimating
the correction.}

\item{calibration_value}{Optional value in \code{calibration_col} identifying
calibration/check rows. If \code{calibration_col} is supplied and
\code{calibration_value = NULL}, logical \code{TRUE} rows are used.}

\item{method}{Shift estimator. \code{"median_shift"} uses median target-minus-gaze
offsets; \code{"mean_shift"} uses mean offsets.}

\item{min_valid_points}{Minimum valid target/gaze pairs required per group.}

\item{max_shift}{Optional maximum Euclidean correction shift. If exceeded,
the shift is reported but not applied.}

\item{output_x_col}{Corrected horizontal gaze output column.}

\item{output_y_col}{Corrected vertical gaze output column.}

\item{dx_col}{Estimated horizontal correction column.}

\item{dy_col}{Estimated vertical correction column.}

\item{shift_col}{Estimated Euclidean shift-distance column.}

\item{error_before_col}{Row-wise gaze-to-target error before correction.}

\item{error_after_col}{Row-wise gaze-to-target error after correction.}

\item{status_col}{Row-level recalibration status column.}

\item{overwrite}{Logical. If \code{FALSE}, existing output columns are protected.}

\item{name}{Character label stored in object attributes.}
}
\value{
A tibble with recalibrated gaze columns and recalibration attributes.
}
\description{
Apply an offline drift-correction shift to Gazepoint gaze coordinates using
known fixation/check-target coordinates. For each group, the helper estimates
the horizontal and vertical gaze offset from valid target samples and applies
the correction to all gaze samples in the same group.
}
\details{
This helper is useful only when known target coordinates are available, for
example from calibration checks, fixation targets, validation targets, or
drift-check trials.
}
