% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_gazepoint_cluster_permutation.R
\name{run_gazepoint_cluster_permutation}
\alias{run_gazepoint_cluster_permutation}
\title{Run paired cluster-based permutation tests}
\usage{
run_gazepoint_cluster_permutation(
  data,
  condition_order = NULL,
  n_permutations = 1000,
  cluster_threshold = 2,
  tail = c("two_sided", "greater", "less"),
  cluster_stat = c("sum_abs_t", "sum_t", "size"),
  min_time_bins = 1,
  seed = NULL,
  paired = TRUE
)
}
\arguments{
\item{data}{Cluster-ready data produced by
\code{prepare_gazepoint_cluster_data()}.}

\item{condition_order}{Optional character vector of length 2 defining the
two conditions and their order. The tested difference is condition 2 minus
condition 1.}

\item{n_permutations}{Number of sign-flip permutations.}

\item{cluster_threshold}{Absolute t-statistic threshold for forming
candidate clusters. For \code{tail = "greater"} or \code{tail = "less"}, the same
positive threshold is used in the requested direction.}

\item{tail}{Direction of the test. \code{"two_sided"} tests positive and negative
clusters. \code{"greater"} tests condition 2 greater than condition 1.
\code{"less"} tests condition 2 less than condition 1.}

\item{cluster_stat}{Cluster statistic. \code{"sum_abs_t"} sums absolute
t-statistics within a cluster. \code{"sum_t"} sums signed t-statistics and then
uses the absolute value for cluster-level inference. \code{"size"} uses the
number of time bins.}

\item{min_time_bins}{Minimum number of adjacent time bins required for a
cluster to be retained.}

\item{seed}{Optional random seed for reproducible permutations.}

\item{paired}{Logical. Currently only paired within-subject sign-flip
permutation is supported.}
}
\value{
A list containing observed time-course statistics, observed
clusters, the permutation distribution, settings, and status fields.
}
\description{
Run a paired cluster-based permutation test on time-course data prepared by
\code{prepare_gazepoint_cluster_data()}. The function tests whether two
conditions diverge over time while controlling cluster-level inference using
a permutation distribution of maximum cluster statistics.
}
\details{
Cluster-based permutation tests are intended for time-course inference.
They should not be used to discover a confirmatory time window and then test
that same window again in a second confirmatory model.
}
