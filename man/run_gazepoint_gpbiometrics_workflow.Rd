% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpbiometrics_bridge.R
\name{run_gazepoint_gpbiometrics_workflow}
\alias{run_gazepoint_gpbiometrics_workflow}
\title{Run a tested gp3tools-to-gpbiometrics integration workflow}
\usage{
run_gazepoint_gpbiometrics_workflow(
  gaze_data,
  biometrics_data,
  gaze_args = list(),
  biometric_participant_col = NULL,
  biometric_trial_col = NULL,
  biometric_time_col = NULL,
  biometric_time_unit = c("auto", "seconds", "milliseconds"),
  signal_cols = NULL,
  event_col = NULL,
  tolerance_s = NULL,
  adapter = NULL,
  include_unmatched = TRUE
)
}
\arguments{
\item{gaze_data}{Raw gp3tools gaze/master data or a prepared bridge.}

\item{biometrics_data}{Biometric samples, including participant, trial, time,
and one or more numeric signal columns.}

\item{gaze_args}{Named list forwarded to
\code{\link[=prepare_gazepoint_gpbiometrics_bridge]{prepare_gazepoint_gpbiometrics_bridge()}}.}

\item{biometric_participant_col, biometric_trial_col, biometric_time_col}{Explicit biometric source columns.}

\item{biometric_time_unit}{Unit for the biometric time column.}

\item{signal_cols}{Numeric biometric signals to summarise.}

\item{event_col}{Optional biometric event column.}

\item{tolerance_s}{Maximum absolute time difference for a match. When \code{NULL},
it is estimated conservatively from biometric sampling intervals.}

\item{adapter}{Optional function with arguments \code{gaze}, \code{biometrics}, and
\code{tolerance_s}; it must return a synchronized data frame.}

\item{include_unmatched}{Retain unmatched gaze rows.}
}
\value{
A \code{"gazepoint_cross_package_workflow"} object.
}
\description{
Aligns gp3tools gaze output with an already imported biometric data frame,
computes AOI/event-contingent signal summaries, and returns a combined audit
and cautious report text. A user-supplied adapter can replace the native
nearest-timestamp join without making gpbiometrics a mandatory dependency.
}
