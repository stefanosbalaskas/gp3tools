% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_gazepoint_model_leave_one_out.R
\name{run_gazepoint_model_leave_one_out}
\alias{run_gazepoint_model_leave_one_out}
\title{Run leave-one-unit model sensitivity analysis}
\usage{
run_gazepoint_model_leave_one_out(
  data,
  unit_col,
  fit_function,
  extract_function = NULL,
  effect_terms = NULL,
  min_rows = 2L,
  keep_models = FALSE,
  name = "gazepoint_model_leave_one_out"
)
}
\arguments{
\item{data}{A data frame used for model fitting.}

\item{unit_col}{Column identifying the unit to leave out, for example subject,
participant, item, stimulus, or trial.}

\item{fit_function}{Function that takes one data frame argument and returns a
fitted model.}

\item{extract_function}{Optional function that takes a fitted model and
returns a data frame of effects. If \code{NULL}, a default coefficient extractor
is used for common model objects.}

\item{effect_terms}{Optional character vector of terms/effects to retain in
the sensitivity summary.}

\item{min_rows}{Minimum number of rows required after leaving one unit out.}

\item{keep_models}{Logical. If \code{TRUE}, keep the full model and refitted models.}

\item{name}{Character label stored in the returned object.}
}
\value{
A list with class \code{gp3_model_leave_one_out_sensitivity}.
}
\description{
Refit the same model repeatedly while removing one participant, item,
stimulus, trial, or other analysis unit at a time. The helper compares
leave-one-out estimates with the full-data model to assess whether a key
effect is driven by a single unit.
}
\details{
This is a generic robustness wrapper. It can be used with linear models,
GLMs, mixed models, GAMMs, GCA models, AOI GLMMs, pupil LMMs, or any custom
model as long as a fitting function is supplied.
}
