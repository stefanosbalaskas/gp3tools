% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth_gazepoint_pupil.R
\name{smooth_gazepoint_pupil}
\alias{smooth_gazepoint_pupil}
\title{Smooth Gazepoint pupil data}
\usage{
smooth_gazepoint_pupil(
  data,
  pupil_col = NULL,
  time_col = NULL,
  group_cols = c("subject", "media_id"),
  window_samples = 5,
  method = c("mean", "median"),
  align = c("center", "right", "left"),
  min_points = 1,
  preserve_missing = TRUE
)
}
\arguments{
\item{data}{A Gazepoint master table, preferably after pupil preprocessing.}

\item{pupil_col}{Optional name of the pupil column to smooth. If \code{NULL}, the
function detects one of \code{pupil_baseline_corrected},
\code{pupil_baseline_percent_change}, \code{pupil_interpolated},
\code{pupil_for_preprocessing}, \code{mean_pupil}, \code{pupil}, \code{pupil_raw},
\code{left_pupil}, or \code{right_pupil}.}

\item{time_col}{Optional name of the time column. If \code{NULL}, the function
detects one of \code{time_ms}, \code{time}, \code{time_orig}, or \code{time_orig_ms}.}

\item{group_cols}{Character vector of grouping columns used to keep smoothing
within independent time series. Defaults to \code{c("subject", "media_id")}.
Columns such as \code{"trial"} or \code{"trial_global"} can be added when available.
Use \code{character(0)} for global smoothing.}

\item{window_samples}{Number of samples in the rolling smoothing window.
Defaults to \code{5}.}

\item{method}{Smoothing statistic. One of \code{"mean"} or \code{"median"}. Defaults
to \code{"mean"}.}

\item{align}{Window alignment. One of \code{"center"}, \code{"right"}, or \code{"left"}.
Defaults to \code{"center"}.}

\item{min_points}{Minimum number of finite values required inside a window to
return a smoothed value. Defaults to \code{1}.}

\item{preserve_missing}{Logical. If \code{TRUE}, rows with missing/non-finite input
remain missing in \code{pupil_smoothed}. Defaults to \code{TRUE}.}
}
\value{
A tibble containing the original data plus pupil-smoothing columns.
}
\description{
Applies sample-based rolling smoothing to a Gazepoint pupil time series,
typically after \code{\link[=flag_gazepoint_pupil]{flag_gazepoint_pupil()}}, \code{\link[=interpolate_gazepoint_pupil]{interpolate_gazepoint_pupil()}},
and optionally \code{\link[=baseline_correct_gazepoint_pupil]{baseline_correct_gazepoint_pupil()}}. The function preserves
the original pupil column and adds smoothed-output columns.
}
\examples{
\dontrun{
smoothed <- smooth_gazepoint_pupil(
  baseline_corrected,
  pupil_col = "pupil_baseline_corrected",
  window_samples = 5,
  method = "mean"
)

dplyr::count(smoothed, pupil_smoothing_status)
}

}
