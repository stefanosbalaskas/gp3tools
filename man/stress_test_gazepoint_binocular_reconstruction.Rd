% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binocular_pupil_validation.R
\name{stress_test_gazepoint_binocular_reconstruction}
\alias{stress_test_gazepoint_binocular_reconstruction}
\title{Stress-test binocular reconstruction across missingness levels}
\usage{
stress_test_gazepoint_binocular_reconstruction(
  data,
  left_col,
  right_col,
  time_col = NULL,
  group_cols = NULL,
  gap_group_cols = NULL,
  fallback_group_cols = NULL,
  missingness = c(0.05, 0.1, 0.2, 0.3),
  mask_modes = c("random", "contiguous"),
  block_size = 6L,
  repeats = 3L,
  seed = 1L,
  min_pairs = 30L,
  min_unique = 5L,
  min_r2 = NULL,
  max_gap_ms = Inf,
  valid_min = NULL,
  valid_max = NULL
)
}
\arguments{
\item{data, left_col, right_col, time_col, group_cols, gap_group_cols, fallback_group_cols}{See
\code{\link[=validate_gazepoint_binocular_reconstruction]{validate_gazepoint_binocular_reconstruction()}}.}

\item{missingness}{Numeric proportions strictly between 0 and 1.}

\item{mask_modes}{One or both of \code{"random"} and \code{"contiguous"}.}

\item{block_size}{Contiguous mask size in samples.}

\item{repeats}{Repeats per stress-test cell.}

\item{seed}{Base seed; deterministic offsets are used across cells.}

\item{min_pairs, min_unique, min_r2, max_gap_ms, valid_min, valid_max}{Passed to
validation.}
}
\value{
A \code{gp3_binocular_stress_test} object containing cell-level \code{results}
and individual validation objects.
}
\description{
Repeats artificial monocular-loss validation across declared missingness levels
and random/contiguous masking modes to show where cross-eye reconstruction
begins to degrade.
}
\examples{
dat <- simulate_gazepoint_pupil_data(n_subjects = 4, n_trials = 2, seed = 22)
stress_test_gazepoint_binocular_reconstruction(
  dat, "pupil_left", "pupil_right", time_col = "timestamp_ms",
  group_cols = "subject", missingness = c(0.05, 0.10),
  mask_modes = "random", repeats = 1, min_pairs = 20
)

}
\seealso{
\code{\link[=validate_gazepoint_binocular_reconstruction]{validate_gazepoint_binocular_reconstruction()}}
}
