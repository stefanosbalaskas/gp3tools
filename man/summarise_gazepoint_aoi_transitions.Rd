% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_gazepoint_aoi_transitions.R
\name{summarise_gazepoint_aoi_transitions}
\alias{summarise_gazepoint_aoi_transitions}
\title{Summarise Gazepoint AOI transition features}
\usage{
summarise_gazepoint_aoi_transitions(
  data,
  aoi_col = NULL,
  time_col = "time",
  group_cols = c("subject", "MEDIA_ID", "trial_global"),
  include_non_aoi = TRUE,
  target_aoi_values = NULL,
  distractor_aoi_values = NULL,
  non_aoi_values = c("non_aoi", "none", "background", "outside", "outside_aoi",
    "missing", "missing_aoi"),
  missing_aoi_label = "missing_aoi"
)
}
\arguments{
\item{data}{A Gazepoint sample-level data frame, AOI-entry table, or
AOI-sequence table.}

\item{aoi_col}{Name of the AOI-state column. Used only when \code{data} is
sample-level data. If \code{NULL}, the function tries \code{aoi_current}, \code{AOI},
and \code{aoi_state}.}

\item{time_col}{Name of the time column, in milliseconds. Used only when
\code{data} is sample-level data.}

\item{group_cols}{Character vector of columns defining independent AOI
sequences, usually subject/media/trial.}

\item{include_non_aoi}{Logical. If \code{TRUE}, non-AOI/background states are
retained before transition summaries are computed. This is useful for
background-to-target and target-to-background features.}

\item{target_aoi_values}{Optional character vector defining target AOI labels.}

\item{distractor_aoi_values}{Optional character vector defining distractor AOI
labels.}

\item{non_aoi_values}{Character vector of AOI labels treated as background
or non-AOI states.}

\item{missing_aoi_label}{Label used when the AOI value is missing.}
}
\value{
A tibble with one row per group and trial-level AOI transition
features.
}
\description{
Summarise AOI transitions at the trial or group level. The function can work
from sample-level Gazepoint AOI data, AOI-entry tables created by
\code{summarise_gazepoint_aoi_entries()}, or AOI-sequence tables created by
\code{prepare_gazepoint_aoi_sequences()}.
}
