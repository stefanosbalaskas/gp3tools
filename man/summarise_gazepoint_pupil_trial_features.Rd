% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_gazepoint_pupil_trial_features.R
\name{summarise_gazepoint_pupil_trial_features}
\alias{summarise_gazepoint_pupil_trial_features}
\title{Summarise Gazepoint pupil trial-level features}
\usage{
summarise_gazepoint_pupil_trial_features(
  data,
  group_cols = c("subject", "trial_global"),
  pupil_col = NULL,
  time_col = "time",
  interpolated_col = "pupil_was_interpolated",
  artifact_col = NULL,
  artifact_reason_col = NULL,
  early_window = c(0, 500),
  middle_window = c(500, 1500),
  late_window = c(1500, 3000),
  min_valid_samples = 1
)
}
\arguments{
\item{data}{A Gazepoint pupil data frame.}

\item{group_cols}{Character vector of grouping columns. The default is
\code{c("subject", "trial_global")}.}

\item{pupil_col}{Name of the processed pupil column to summarise. If \code{NULL},
the function tries \code{pupil_smoothed}, \code{pupil_baseline_corrected},
\code{pupil_baseline_percent_change}, \code{pupil_interpolated}, \code{pupil_clean},
and \code{pupil}.}

\item{time_col}{Name of the time column.}

\item{interpolated_col}{Optional logical interpolation flag column.}

\item{artifact_col}{Optional artifact flag column. If \code{NULL}, the function
tries to detect \code{pupil_artifact_flag}, \code{pupil_flag_invalid}, or
\code{artifact_flag}.}

\item{artifact_reason_col}{Optional artifact-reason column. If \code{NULL}, the
function tries to detect \code{pupil_artifact_reason}, \code{pupil_flag_reason}, or
\code{artifact_reason}.}

\item{early_window}{Numeric vector of length 2 defining the early window in
milliseconds.}

\item{middle_window}{Numeric vector of length 2 defining the middle window in
milliseconds.}

\item{late_window}{Numeric vector of length 2 defining the late window in
milliseconds.}

\item{min_valid_samples}{Minimum number of valid pupil samples required for
a trial to be labelled \code{"ok"}.}
}
\value{
A tibble with one row per trial/group.
}
\description{
Convert sample-level Gazepoint pupil time series into trial-level pupil
features for statistical modelling.
}
\details{
The function summarises one row per trial or other user-defined grouping.
It computes mean pupil, peak pupil, time-to-peak, AUC, early/middle/late
window means, valid-sample percentage, interpolation percentage, artifact
percentage, and missingness summaries.
}
