% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_gazepoint_pupil_windows.R
\name{summarise_gazepoint_pupil_windows}
\alias{summarise_gazepoint_pupil_windows}
\title{Summarise Gazepoint pupil responses within time windows}
\usage{
summarise_gazepoint_pupil_windows(
  data,
  pupil_col = NULL,
  time_col = NULL,
  windows = c(0, 500, 1000, 2000),
  group_cols = c("subject", "media_id"),
  include_window_end = FALSE,
  min_valid_samples = 1
)
}
\arguments{
\item{data}{A Gazepoint master table or processed pupil table.}

\item{pupil_col}{Optional name of the pupil column to summarise. If \code{NULL},
the function detects one of \code{pupil_smoothed},
\code{pupil_baseline_corrected}, \code{pupil_baseline_percent_change},
\code{pupil_interpolated}, \code{pupil_for_preprocessing}, \code{mean_pupil},
\code{pupil}, \code{pupil_raw}, \code{left_pupil}, or \code{right_pupil}.}

\item{time_col}{Optional name of the time column used for assigning samples
to windows. If \code{NULL}, the function detects one of
\code{time_relative_ms}, \code{relative_time_ms}, \code{event_time_ms}, \code{time_ms},
\code{time}, \code{time_orig}, or \code{time_orig_ms}.}

\item{windows}{Window definitions. Either a numeric vector of breakpoints,
such as \code{c(0, 500, 1000, 2000)}, or a data frame with window start and end
columns. Supported names include \code{window_start_ms}, \code{window_start},
\code{start_ms}, or \code{start}, and \code{window_end_ms}, \code{window_end}, \code{end_ms}, or
\code{end}. A \code{window_label} or \code{label} column is optional.}

\item{group_cols}{Character vector of grouping columns. Standard roles such
as \code{"subject"}, \code{"media_id"}, \code{"trial"}, and \code{"trial_global"} are
internally standardised when available. Other columns, such as
\code{"condition"} or \code{"AOI"}, can also be used if present in \code{data}. Use
\code{character(0)} for overall window summaries.}

\item{include_window_end}{Logical. If \code{FALSE}, windows are left-closed and
right-open: \verb{[start, end)}. If \code{TRUE}, the end point is included:
\verb{[start, end]}. Defaults to \code{FALSE}.}

\item{min_valid_samples}{Minimum number of finite pupil samples required for
a window to be labelled \code{"valid"}. Defaults to \code{1}.}
}
\value{
A tibble with one row per group-by-window combination present in the
data.
}
\description{
Aggregates processed Gazepoint pupil data into user-defined analysis windows,
typically after \code{\link[=flag_gazepoint_pupil]{flag_gazepoint_pupil()}}, \code{\link[=interpolate_gazepoint_pupil]{interpolate_gazepoint_pupil()}},
\code{\link[=baseline_correct_gazepoint_pupil]{baseline_correct_gazepoint_pupil()}}, and \code{\link[=smooth_gazepoint_pupil]{smooth_gazepoint_pupil()}}.
The function can summarise raw, interpolated, baseline-corrected,
percent-change, or smoothed pupil columns.
}
\examples{
\dontrun{
pupil_windows <- summarise_gazepoint_pupil_windows(
  smoothed_pupil,
  pupil_col = "pupil_smoothed",
  windows = c(0, 500, 1000, 2000),
  group_cols = c("subject", "media_id")
)
}

}
