% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scanpath_cluster_stability.R
\name{summarise_gazepoint_scanpath_cluster_stability}
\alias{summarise_gazepoint_scanpath_cluster_stability}
\title{Summarise scanpath-cluster stability}
\usage{
summarise_gazepoint_scanpath_cluster_stability(
  x,
  min_pair_coverage = 0.5,
  stable_threshold = 0.75
)
}
\arguments{
\item{x}{An object returned by
\code{\link[=bootstrap_gazepoint_scanpath_clusters]{bootstrap_gazepoint_scanpath_clusters()}}.}

\item{min_pair_coverage}{Minimum proportion of iterations in which a pair
must co-occur before it contributes to summaries.}

\item{stable_threshold}{Within-reference-cluster co-clustering threshold
used to count stable scanpaths.}
}
\value{
An object of class \code{"gp3_scanpath_cluster_stability_summary"}
containing overview, sequence, pairwise, and representative tables.
}
\description{
Convert bootstrap scanpath-clustering output into overview, sequence-level,
pairwise, and representative-stability tables.
}
\examples{
latent <- rep(1:3, each = 2)
d <- outer(
  latent,
  latent,
  FUN = function(x, y) ifelse(x == y, 0.1, 1)
)
diag(d) <- 0
dimnames(d) <- list(LETTERS[1:6], LETTERS[1:6])

stability <- bootstrap_gazepoint_scanpath_clusters(
  d,
  k = 3,
  n_boot = 10,
  seed = 1
)

summarise_gazepoint_scanpath_cluster_stability(
  stability
)
}
