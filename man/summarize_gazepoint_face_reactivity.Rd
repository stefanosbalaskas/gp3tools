% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/face_window_summaries.R
\name{summarize_gazepoint_face_reactivity}
\alias{summarize_gazepoint_face_reactivity}
\title{Summarise facial-behaviour reactivity between two windows}
\usage{
summarize_gazepoint_face_reactivity(
  data,
  baseline_window,
  response_window,
  group_cols = NULL,
  window_col = NULL,
  measure_cols = NULL,
  statistic = c("mean", "median")
)
}
\arguments{
\item{data}{A window-summary table returned by
\code{summarize_gazepoint_face_windows()}.}

\item{baseline_window}{Value in \code{window_col} identifying the baseline window.}

\item{response_window}{Value in \code{window_col} identifying the response window.}

\item{group_cols}{Optional grouping columns.}

\item{window_col}{Column identifying window labels or IDs. Auto-detected when
possible.}

\item{measure_cols}{Measure names or summary columns. If \code{NULL}, columns
ending in the selected statistic suffix are detected automatically.}

\item{statistic}{Statistic used for reactivity. One of \code{"mean"} or
\code{"median"}.}
}
\value{
A tibble with one row per group and measure. The returned object has
class \code{gp3_face_reactivity_summary}.
}
\description{
Computes baseline-to-response differences from
\code{summarize_gazepoint_face_windows()} output. The function returns one row per
group and measure. Reactivity is reported as response minus baseline. The
helper summarises technical facial-behaviour measures only and does not infer
emotional states.
}
\examples{
face <- data.frame(
  participant_id = "P001",
  face_time_sec = c(0.00, 0.05, 0.10),
  face_valid = c(TRUE, TRUE, TRUE),
  AU12_r = c(0.1, 0.2, 0.3)
)

windows <- data.frame(
  participant_id = "P001",
  window = c("baseline", "response"),
  window_start_sec = c(0.00, 0.05),
  window_end_sec = c(0.05, 0.15)
)

summary <- summarize_gazepoint_face_windows(
  face,
  windows = windows,
  group_cols = "participant_id",
  window_label_col = "window"
)

summarize_gazepoint_face_reactivity(
  summary,
  baseline_window = "baseline",
  response_window = "response",
  group_cols = "participant_id"
)
}
