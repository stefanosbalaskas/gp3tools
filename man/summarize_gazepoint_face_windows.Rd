% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/face_window_summaries.R
\name{summarize_gazepoint_face_windows}
\alias{summarize_gazepoint_face_windows}
\title{Summarise external facial-behaviour data within analysis windows}
\usage{
summarize_gazepoint_face_windows(
  data,
  windows = NULL,
  time_col = NULL,
  window_start_col = "window_start_sec",
  window_end_col = "window_end_sec",
  group_cols = NULL,
  window_id_col = NULL,
  window_label_col = NULL,
  measure_cols = NULL,
  validity_col = NULL,
  confidence_col = NULL,
  require_valid = TRUE,
  include_empty_windows = TRUE
)
}
\arguments{
\item{data}{A face-analysis data frame, usually returned by
\code{standardize_gazepoint_face_columns()} or \code{sync_gazepoint_face_data()}.}

\item{windows}{Optional data frame defining windows. When supplied, it must
contain start and end columns.}

\item{time_col}{Time column in \code{data}, in seconds. Auto-detected when
possible.}

\item{window_start_col}{Window-start column in seconds.}

\item{window_end_col}{Window-end column in seconds.}

\item{group_cols}{Optional grouping columns shared by \code{data} and \code{windows}.}

\item{window_id_col}{Optional window identifier column.}

\item{window_label_col}{Optional human-readable window label column.}

\item{measure_cols}{Numeric facial-behaviour columns to summarise. When
\code{NULL}, likely facial-behaviour columns are detected automatically.}

\item{validity_col}{Optional validity column. Auto-detected when possible.}

\item{confidence_col}{Optional confidence column. Auto-detected when possible.}

\item{require_valid}{Should measure summaries use only rows where the validity
column is \code{TRUE} when such a column is available?}

\item{include_empty_windows}{Should windows with no matching rows be kept in
the output?}
}
\value{
A tibble with one row per group/window and summary columns for each
measure. The returned object has class \code{gp3_face_window_summary}.
}
\description{
Summarises numeric external facial-behaviour variables within time windows.
The helper can use a separate window table or data rows that already contain
window labels. It is intended for external face-analysis data imported,
standardised, and optionally synchronised with Gazepoint data. It does not
infer facial expressions or emotional states.
}
\examples{
face <- data.frame(
  participant_id = "P001",
  face_time_sec = c(0.00, 0.05, 0.10),
  face_confidence = c(0.95, 0.94, 0.93),
  face_valid = c(TRUE, TRUE, TRUE),
  AU12_r = c(0.1, 0.2, 0.3)
)

windows <- data.frame(
  participant_id = "P001",
  window = c("baseline", "response"),
  window_start_sec = c(0.00, 0.05),
  window_end_sec = c(0.05, 0.15)
)

summarize_gazepoint_face_windows(
  face,
  windows = windows,
  group_cols = "participant_id",
  window_label_col = "window"
)
}
