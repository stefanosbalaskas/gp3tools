% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/face_sync.R
\name{sync_gazepoint_face_data}
\alias{sync_gazepoint_face_data}
\title{Synchronise external facial-behaviour data with Gazepoint data}
\usage{
sync_gazepoint_face_data(
  gazepoint_data,
  face_data,
  method = c("nearest_time", "frame_exact"),
  by = NULL,
  gaze_time_col = NULL,
  face_time_col = NULL,
  gaze_frame_col = NULL,
  face_frame_col = NULL,
  tolerance_sec = 0.05,
  prefix = "face_",
  keep_unmatched = TRUE,
  standardize_face = TRUE
)
}
\arguments{
\item{gazepoint_data}{Gazepoint data frame, typically a master table, trial
table, or sample-level table.}

\item{face_data}{External facial-behaviour data frame, preferably returned by
\code{standardize_gazepoint_face_columns()}.}

\item{method}{Synchronisation method. \code{"nearest_time"} matches each Gazepoint
row to the nearest facial-data row within group. \code{"frame_exact"} matches by
exact frame index within group.}

\item{by}{Optional named character vector mapping Gazepoint grouping columns
to facial-data grouping columns. For example,
\code{c(subject_id = "participant_id")}. Use \code{NULL} for no grouping.}

\item{gaze_time_col}{Gazepoint time column. Required for
\code{method = "nearest_time"} unless auto-detected.}

\item{face_time_col}{Facial-data time column. Defaults to \code{face_time_sec}
when available.}

\item{gaze_frame_col}{Gazepoint frame column. Required for
\code{method = "frame_exact"} unless auto-detected.}

\item{face_frame_col}{Facial-data frame column. Defaults to \code{face_frame}
when available.}

\item{tolerance_sec}{Maximum allowed absolute time difference in seconds for
nearest-time matching.}

\item{prefix}{Prefix added to non-standard facial-data columns before joining.}

\item{keep_unmatched}{Should Gazepoint rows without a valid face-data match be
retained?}

\item{standardize_face}{Should \code{face_data} be passed through
\code{standardize_gazepoint_face_columns()} before synchronisation when needed?}
}
\value{
A tibble with Gazepoint columns plus matched facial-behaviour columns
and synchronisation metadata. The returned object has class
\code{gp3_face_sync}.
}
\description{
Aligns external facial-behaviour data to Gazepoint rows using either nearest
time matching or exact frame matching. The helper is designed for facial data
previously imported with \code{read_gazepoint_face_export()} and standardised with
\code{standardize_gazepoint_face_columns()}. It does not infer facial expressions
from Gazepoint exports and does not interpret facial behaviour as emotion.
}
\examples{
gaze <- data.frame(
  subject_id = "P001",
  time_sec = c(0.00, 0.03, 0.07),
  AOI = c("A", "A", "B")
)

face <- data.frame(
  participant_id = "P001",
  frame = 1:3,
  timestamp = c(0.00, 0.033, 0.066),
  confidence = c(0.95, 0.94, 0.93),
  success = c(1, 1, 1),
  AU12_r = c(0.1, 0.2, 0.3)
)

sync_gazepoint_face_data(
  gaze,
  face,
  by = c(subject_id = "participant_id"),
  gaze_time_col = "time_sec"
)
}
