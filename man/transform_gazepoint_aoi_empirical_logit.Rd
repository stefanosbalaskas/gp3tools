% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform_gazepoint_aoi_empirical_logit.R
\name{transform_gazepoint_aoi_empirical_logit}
\alias{transform_gazepoint_aoi_empirical_logit}
\title{Transform AOI proportions to empirical logits}
\usage{
transform_gazepoint_aoi_empirical_logit(
  data,
  numerator_col = NULL,
  denominator_col = NULL,
  proportion_col = NULL,
  correction = 0.5,
  pseudo_denominator = 1,
  output_col = "aoi_empirical_logit",
  adjusted_proportion_col = "aoi_proportion_adjusted",
  raw_proportion_col = "aoi_proportion_raw",
  numerator_output_col = "aoi_numerator",
  denominator_output_col = "aoi_denominator",
  status_col = "aoi_empirical_logit_status",
  overwrite = FALSE,
  name = "gazepoint_aoi_empirical_logit"
)
}
\arguments{
\item{data}{A data frame containing AOI proportions or AOI count data.}

\item{numerator_col}{Optional numerator column, for example number of samples
or fixations inside the AOI.}

\item{denominator_col}{Optional denominator column, for example total valid
samples or total fixations in the window.}

\item{proportion_col}{Optional bounded AOI proportion column. If
\code{numerator_col} and \code{denominator_col} are supplied, the raw proportion is
computed from those columns. If only \code{proportion_col} is supplied, a
pseudo-denominator is used and recorded in the output.}

\item{correction}{Positive correction constant added to numerator and
non-AOI count. The common empirical-logit correction is \code{0.5}.}

\item{pseudo_denominator}{Positive pseudo-denominator used only when
\code{proportion_col} is supplied without \code{denominator_col}.}

\item{output_col}{Name of the empirical-logit output column.}

\item{adjusted_proportion_col}{Name of the adjusted proportion output column.}

\item{raw_proportion_col}{Name of the raw proportion output column.}

\item{numerator_output_col}{Name of the numerator output column used in the
transformation.}

\item{denominator_output_col}{Name of the denominator output column used in
the transformation.}

\item{status_col}{Name of the row-level transformation status column.}

\item{overwrite}{Logical. If \code{FALSE}, the function errors when output columns
already exist in \code{data}.}

\item{name}{Character label stored in object attributes.}
}
\value{
A tibble with empirical-logit transformation columns added. The object
has class \code{gp3_aoi_empirical_logit_data}.
}
\description{
Convert bounded AOI proportions into empirical logits for linear mixed models,
growth-curve analysis, or other approximately Gaussian time-course models.
}
\details{
Binomial GLMMs are usually preferable when numerator and denominator counts
are available. This helper is intended for sensitivity analyses, GCA-style
models, and linear time-course summaries where a transformed AOI proportion
is needed.
}
