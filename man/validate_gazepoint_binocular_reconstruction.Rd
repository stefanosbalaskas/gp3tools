% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binocular_pupil_validation.R
\name{validate_gazepoint_binocular_reconstruction}
\alias{validate_gazepoint_binocular_reconstruction}
\title{Validate binocular reconstruction using artificial monocular loss}
\usage{
validate_gazepoint_binocular_reconstruction(
  data,
  left_col,
  right_col,
  time_col = NULL,
  group_cols = NULL,
  gap_group_cols = NULL,
  fallback_group_cols = NULL,
  direction = c("both", "left_from_right", "right_from_left"),
  mask_prop = 0.2,
  mask_mode = c("random", "contiguous"),
  block_size = 6L,
  repeats = 5L,
  seed = 1L,
  min_pairs = 30L,
  min_unique = 5L,
  min_r2 = NULL,
  time_unit = c("auto", "milliseconds", "seconds"),
  max_gap_ms = Inf,
  allow_edge_gaps = TRUE,
  allow_extrapolation = FALSE,
  valid_min = NULL,
  valid_max = NULL
)
}
\arguments{
\item{data}{A data frame containing binocular pupil measurements.}

\item{left_col, right_col}{Numeric pupil columns.}

\item{time_col}{Optional time column; required for meaningful contiguous-gap
validation and finite \code{max_gap_ms}.}

\item{group_cols}{Primary calibration and masking groups.}

\item{gap_group_cols}{Optional groups used to define temporal missing-eye runs
during reconstruction; defaults to \code{group_cols}.}

\item{fallback_group_cols}{Optional calibration fallback groups.}

\item{direction}{\code{"both"}, \code{"left_from_right"}, or \code{"right_from_left"}.}

\item{mask_prop}{Proportion of bilateral observations masked in each repeat.}

\item{mask_mode}{\code{"random"} masks individual observations; \code{"contiguous"}
masks short ordered runs.}

\item{block_size}{Number of samples per attempted contiguous block.}

\item{repeats}{Number of repeated artificial-missingness evaluations.}

\item{seed}{Random seed. The caller's RNG state is restored on exit.}

\item{min_pairs, min_unique, min_r2}{Calibration gates.}

\item{time_unit}{Time unit used by reconstruction.}

\item{max_gap_ms, allow_edge_gaps, allow_extrapolation}{Reconstruction gates.}

\item{valid_min, valid_max}{Optional bounds.}
}
\value{
A \code{gp3_binocular_validation} object containing repeat-level \code{metrics},
row-level \code{predictions}, aggregated \code{summary}, and settings.
}
\description{
Uses bilateral observations as known reference values, temporarily masks one
eye, refits cross-eye calibration without the masked target values, reconstructs
the hidden observations, and compares predictions with the held-out values.
This provides dataset-specific empirical reconstruction diagnostics rather than
assuming that a cross-eye regression is adequate.
}
\details{
Artificial masking evaluates prediction error where the hidden target
is actually known. It does not prove that naturally missing observations are
missing at random or that their unobserved values would follow the same error
distribution.
}
\examples{
dat <- simulate_gazepoint_pupil_data(n_subjects = 5, n_trials = 2, seed = 21)
val <- validate_gazepoint_binocular_reconstruction(
  dat, "pupil_left", "pupil_right", time_col = "timestamp_ms",
  group_cols = "subject", mask_prop = 0.1, repeats = 2,
  min_pairs = 20, seed = 9
)
val$summary

}
\seealso{
\code{\link[=fit_gazepoint_binocular_calibration]{fit_gazepoint_binocular_calibration()}},
\code{\link[=stress_test_gazepoint_binocular_reconstruction]{stress_test_gazepoint_binocular_reconstruction()}}
}
