% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_gazepoint_master.R
\name{validate_gazepoint_master}
\alias{validate_gazepoint_master}
\title{Validate a Gazepoint master sample table}
\usage{
validate_gazepoint_master(
  master,
  min_valid_sample_pct = 75,
  max_missing_gaze_pct = 25,
  max_missing_pupil_pct = 50,
  max_offscreen_gaze_pct = 25,
  require_pupil = FALSE,
  require_aoi = FALSE,
  fail_on_error = FALSE
)
}
\arguments{
\item{master}{A Gazepoint master sample-level table.}

\item{min_valid_sample_pct}{Minimum acceptable percentage of valid gaze
samples. Defaults to \code{75}.}

\item{max_missing_gaze_pct}{Maximum acceptable percentage of missing gaze
samples. Defaults to \code{25}.}

\item{max_missing_pupil_pct}{Maximum acceptable percentage of missing pupil
samples. Defaults to \code{50}.}

\item{max_offscreen_gaze_pct}{Maximum acceptable percentage of off-screen gaze
samples. Defaults to \code{25}.}

\item{require_pupil}{Logical. If \code{TRUE}, the validation fails when no usable
pupil column is present. Defaults to \code{FALSE}.}

\item{require_aoi}{Logical. If \code{TRUE}, the validation fails when no real AOI
samples are present. Defaults to \code{FALSE}.}

\item{fail_on_error}{Logical. If \code{TRUE}, the function aborts when one or more
validation checks fail. Defaults to \code{FALSE}.}
}
\value{
A named list with:
\describe{
\item{summary}{One-row validation summary.}
\item{checks}{A tibble containing all validation checks.}
\item{failed_checks}{Validation checks with status \code{"fail"}.}
\item{warning_checks}{Validation checks with status \code{"warning"}.}
\item{column_map}{Detected column mapping used for validation.}
}
}
\description{
Performs formal validation checks on a Gazepoint master sample-level table
created by \code{\link[=as_gazepoint_master]{as_gazepoint_master()}} or \code{\link[=create_gazepoint_master]{create_gazepoint_master()}}. This
function is intended as a gate between master-table construction and more
advanced steps such as pupil preprocessing, AOI modelling, or statistical
analysis.
}
\examples{
\donttest{
master <- gazepoint_example_master
master <- create_gazepoint_master(
  gaze_data = gazepoint_example_master,
  screen_width_px = 1920,
  screen_height_px = 1080
)

validation <- validate_gazepoint_master(master)

validation$summary
validation$checks
}

}
