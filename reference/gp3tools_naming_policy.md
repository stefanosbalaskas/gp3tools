# gp3tools user-facing naming policy

Documents the package policy that British English `summarise_*` names
are canonical for new summary helpers. Existing American English
`summarize_*` exports remain available for backward compatibility.

## Usage

``` r
gp3tools_naming_policy()
```

## Value

A list describing the policy and migration rules.
